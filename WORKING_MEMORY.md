# Working Memory

- 2026-03-24T03:08:23Z: Completed the next epic `#21` tool-discovery compatibility slice on branch `feature/issue-21-mcp-discoverability`. Kept the public MCP surface at one tool but made `guild.inspect` more explicit in the wire shape: initialize now advertises `tools.listChanged = false`, tool metadata now carries both top-level and annotation titles plus explicit `execution.taskSupport = forbidden`, and the local protocol types now model those optional 2025-11-25 tool fields rather than the older narrower shape. Updated the repo-local stdio proof, README/command-language/recipe/testing docs, and CLI/docs regressions so the expected discovery-first flow is now `initialize -> tools/list -> resources/list -> resources/read -> resources/templates/list -> guild.inspect`, and a client rendering `Tools: (none)` is treated as a compatibility regression rather than the intended Guild surface. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test mcp_server_stdio`, `cargo run -p guild-mcp --example mcp_stdio_local`, `cargo test -p guild-mcp --test guild_cli mcp_stdio_launches_through_guild_cli -- --exact`, `cargo test -p guild-mcp --test guild_cli journey_docs_stay_centered_on_user_workflows -- --exact`, `cargo test -p guild-mcp --test guild_cli mcp_contract_docs_match_the_discovery_catalog_surface -- --exact`, and `git diff --check`.

- 2026-03-24T02:45:38Z: Completed the next MCP wording cleanup slice for epic `#21` on branch `feature/issue-21-mcp-discoverability`. Tightened evidence metadata descriptions in the stdio MCP server so the producing execution linkage now renders as the canonical `guild://executions/<id>` URI instead of implying the stored `produced_by_execution` field already contains a full URI. This keeps the agent-facing metadata aligned with the URI-first discovery flow while preserving the underlying evidence-record contract. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test mcp_server_stdio guild_inspect_success_returns_structured_content_text_and_resource_links -- --exact`, `cargo test -p guild-mcp --test mcp_server_stdio resources_templates_pagination_and_resources_list_match_active_resource_model -- --exact`, and `git diff --check`.

- 2026-03-24T02:41:18Z: Completed the PR `#42` contract-surface follow-up for epic `#21` on branch `feature/issue-21-mcp-discoverability`. Addressed the review pointing out that `resources/list` had outpaced the normative docs by updating `SPECS.md` and `ARCHITECTURE.md` to describe the shipped bounded discovery-catalog contract: canonical recent-execution query entry points first, then recent execution resources, then recent evidence-metadata resources, with evidence payload/blob URIs still discoverable through templates and links rather than default listing. Tightened the parallel README and command-language wording to call out that the first `resources/list` entries are query URIs, and added a focused docs regression in `crates/guild-mcp/tests/guild_cli.rs` so the contract/docs drift does not regress. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli journey_docs_stay_centered_on_user_workflows -- --exact`, `cargo test -p guild-mcp --test guild_cli mcp_contract_docs_match_the_discovery_catalog_surface -- --exact`, and `git diff --check`.

- 2026-03-24T02:36:58Z: Completed the child-execution discoverability slice for epic `#21` on branch `feature/issue-21-mcp-discoverability`. `guild.inspect` MCP results now surface child execution URIs for composite runs as first-class `ResourceLink` content blocks alongside the parent execution and evidence links, so agent clients can follow nested execution paths without first parsing the JSON envelope. Added a focused stdio regression that installs `hello-composite`, grants its typed `invoke-skill` dependency alias, and asserts the returned content includes the child execution link with honest alias/target/status wording. Revalidated with `cargo fmt --all` and `cargo test -p guild-mcp --test mcp_server_stdio`.

- 2026-03-24T02:32:25Z: Started the next agent-discoverability slice for epic `#21` on branch `feature/issue-21-mcp-discoverability`: expose child execution URIs as first-class MCP `ResourceLink`s in inspect results. The structured execution record already carries child execution refs for composite skills, but agent clients still have to parse the JSON envelope to follow them; this pass is making the nested execution path visible in the same content surface that already exposes parent execution and evidence links.

- 2026-03-24T02:30:19Z: Completed the discovery-first MCP proof/example slice for epic `#21` on branch `feature/issue-21-mcp-discoverability`. `crates/guild-mcp/examples/mcp_stdio_local.rs` now proves the same workflow the server/docs teach: start from `resources/list`, read the canonical recent-executions query through `resources/read`, paginate `resources/templates/list` for custom URI families, execute `guild.inspect`, then rediscover and read the persisted execution and evidence metadata resources. Updated `docs/testing.md` to describe that proof honestly. Revalidated with `cargo fmt --all`, `cargo run -p guild-mcp --example mcp_stdio_local`, `cargo test -p guild-mcp --test guild_cli user_facing_docs_use_installed_guild_cli_after_install -- --exact`, and `git diff --check`.

- 2026-03-24T02:26:58Z: Started the next proof/docs slice for epic `#21` on branch `feature/issue-21-mcp-discoverability`. The live server metadata and recipe docs are ahead of the repo-local MCP proof example, so this pass is updating `crates/guild-mcp/examples/mcp_stdio_local.rs` and `docs/testing.md` to demonstrate the new discovery-first agent path honestly: `resources/list` -> `resources/read` on canonical query URIs -> `resources/templates/list` for custom families -> `guild.inspect` -> read back the discovered execution/evidence resources.

- 2026-03-24T02:25:05Z: Completed the next MCP metadata audit slice for epic `#21` on branch `feature/issue-21-mcp-discoverability`. Tightened resource-template wording so payload-vs-metadata-vs-blob reads explain the safer path first, and upgraded live execution/evidence resource links plus listed resources to carry real resolved-skill, status, MIME, audience, redaction, and producing-execution context instead of generic descriptions. Added focused stdio assertions for the new descriptions, metadata sizes, and template wording. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test mcp_server_stdio`, and `git diff --check`.

- 2026-03-24T02:19:09Z: Completed the agent-docs slice for epic `#21` on branch `feature/issue-21-mcp-discoverability`. Added `docs/mcp-agent-recipes.md` as the concrete task-shaped MCP guide for agent users, linked it from the top-level MCP docs in README and `docs/command-language.md`, and extended the existing docs regression so the new page is covered by the installed-CLI and user-journey expectations. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-24T02:15:51Z: Started the next epic `#21` slice on branch `feature/issue-21-mcp-discoverability`: add a concrete agent-facing MCP recipe doc and wire it into the existing user docs/tests. The goal is to close the remaining acceptance gap around task-shaped agent docs by teaching four practical flows directly: inspect a skill, find an execution, fetch evidence safely, and explain a failure.

- 2026-03-24T02:14:05Z: Completed the PR `#42` self-review follow-up on branch `feature/issue-21-mcp-discoverability`. Tightened the heavy `resources/list` pagination regression to use a 13-run mixed-resource fixture instead of 55 inspect executions while keeping the same second-page, ordering, and cursor assertions for the new query-plus-execution-plus-evidence metadata model. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test mcp_server_stdio`, and `git diff --check`; the stdio suite runtime dropped from roughly 5+ minutes to about 1 minute on this branch.

- 2026-03-24T02:12:28Z: Started a follow-up self-review pass on branch `feature/issue-21-mcp-discoverability` after opening PR `#42`. There were no GitHub review comments yet, so the first concrete gap is performance-oriented: the new mixed `resources/list` pagination regression still creates 55 real inspect executions even though the updated resource model only needs slightly more than one page of results to prove cursor behavior. Tightening that fixture count should preserve coverage while materially shrinking the stdio suite runtime.

- 2026-03-24T02:06:33Z: Completed epic `#21` first discoverability slice on branch `feature/issue-21-mcp-discoverability`. The stdio MCP server now teaches the agent workflow in `initialize`, `tools/list`, and docs; `resources/list` now exposes two canonical bounded execution-query entry points followed by recent execution resources and recent evidence metadata resources; and successful inspect results now include evidence metadata links alongside payload links. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test mcp_server_stdio`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-24T01:57:16Z: Started epic `#21` first implementation slice on branch `feature/issue-21-mcp-discoverability`. Scope is the bounded MCP discoverability pass planned from the issue graph: make `resources/list` agent-useful by mixing canonical query entry points with recent executions and recent evidence metadata, add evidence metadata links to inspect results, tighten stdio MCP initialize/tool/resource wording, and keep the change within the existing MCP/runtime/resource substrate.

- 2026-03-24T01:54:00Z: Finished the strict lint cleanup pass on branch `feature/issue-38-why-lineage`. Shrunk `guild-mcp` `CliError` storage so strict `clippy::result_large_err` stops cascading across the CLI surface, split a few flagged `show` / lineage / presenter helpers into smaller or cleaner forms, fixed unreadable numeric literals and missing doc `# Errors` text in `guild-types` / `guild-registry`, and kept the tree formatting-clean. Revalidated with `cargo fmt --all`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test -p guild-mcp --lib`, and `git diff --check`.

- 2026-03-24T01:18:11Z: Started a strict lint cleanup pass on branch `feature/issue-38-why-lineage` after landing issue `#38`. A full `cargo clippy --workspace --all-targets --all-features -- -D warnings` pass surfaced one small cross-crate style fix in `guild-types`, one registry doc-comment fix in `guild-registry`, and a larger existing `guild-mcp` lint backlog concentrated in `CliError` size (`result_large_err`) plus a handful of local `cli.rs` / `cli_presenter.rs` structure/style issues. The cleanup pass is staying contract-neutral: shrink the CLI error storage, refactor the flagged helpers without widening public surface, then rerun the full lint gate.

- 2026-03-24T01:03:47Z: Completed issue `#38` on branch `feature/issue-38-why-lineage`. Added a human-only `guild why --lineage` mode that keeps the default persisted-execution explanation compact, rejects `--json` and `--porcelain`, and appends a bounded read-only ancestry/descendant execution view with `-v` warning detail and `-vv` full execution URIs. Updated the presenter/test surface for lineage rendering, refreshed the explain-path docs to teach `guild why`, `guild why -v`, and `guild why --lineage` before the reusable tree/report skills, and revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --lib`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-24T00:47:50Z: Started implementation for issue `#38` on branch `feature/issue-38-why-lineage`. Scope is intentionally narrow and read-only: add a native human-only `guild why --lineage` view that walks persisted ancestor/descendant execution lineage with bounded traversal, keeps default `guild why` output compact, leaves `--json` and `--porcelain` contracts unchanged, and updates the explain-path docs/tests around the new lineage workflow.

- 2026-03-23T21:08:00Z: Added a CI resource-budget fix on branch `feature/issue-19-why-navigation` after repeated GitHub Actions `verify` failures on runs `#23458803578` and `#23459857004`. The `Test` step was failing on the hosted Ubuntu runner while building the full workspace graph, first with `No space left on device` and then with a linker `Bus error` while linking a large `guild-mcp` example binary. Updated `.github/workflows/ci.yml` to free runner disk before the Rust build and to reduce CI artifact pressure by setting `CARGO_PROFILE_DEV_DEBUG=0`, `CARGO_PROFILE_TEST_DEBUG=0`, and `CARGO_BUILD_JOBS=2` for the verify job. YAML parse validation and `git diff --check` passed locally; the real proof for this change is the next GitHub Actions run on the branch.

- 2026-03-23T20:59:23Z: Addressed all active PR `#37` review threads on branch `feature/issue-19-why-navigation`. The compact human `guild why` path now prints at most one nearby short ref by preferring a child ref over evidence when both exist, `Next:` guidance now uses stable full execution/evidence URIs instead of timestamp-heavy short prefixes, and verbose nearby child/evidence lists no longer truncate at three entries. Added presenter unit coverage for compact-vs-verbose nearby-ref behavior and updated CLI integration expectations for the stable follow-up commands. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --lib cli_presenter::tests`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T20:52:38Z: Closed the remaining issue `#19` conformance gaps on branch `feature/issue-19-why-navigation`. `guild why` now summarizes stored authority observations by default and expands them under `-v`, the user-facing explain docs now teach `guild why -v` plus `guild ls evidence --limit 5` as the explicit stored-evidence discovery path, and `crates/guild-mcp/tests/guild_cli.rs` now locks both exercised and blocked authority-observation rendering into the CLI regression suite. Validation passed with `cargo fmt --all`, `git diff --check`, and `cargo test -p guild-mcp --test guild_cli`. Decomposed epic `#19` into concrete child tasks `#38`, `#39`, and `#40` so the remaining lineage, evidence-discovery, and authority-debug follow-up work is tracked explicitly under the epic.

- 2026-03-23T20:29:58Z: Completed issue `#19` first slice on branch `feature/issue-19-why-navigation`. The human `guild why` surface now reports child/evidence counts, shows one nearby short ref by default, expands nearby child/evidence ref lists under `-v`, and prefers child-lineage follow-up hints over evidence when both exist, while `--json` and `--porcelain` remain unchanged. Updated README, `docs/command-language.md`, and `docs/how-guild-works.md` to teach `guild why` as the first nearby-ref navigation surface over stored executions. Validation passed with `cargo fmt --all`, `git diff --check`, and `cargo test -p guild-mcp --test guild_cli`.

- 2026-03-23T20:21:41Z: Started issue `#19` first implementation slice on branch `feature/issue-19-why-navigation`. Scope is intentionally narrow and explain-path only: improve the human `guild why` workflow so one stored execution can point operators at nearby child executions and emitted evidence without changing the persisted execution contract, MCP surface, or `--json` / `--porcelain` output shapes.

- 2026-03-23T19:35:21Z: Addressed the first open PR `#36` review thread on branch `feat/18-trust-review-surfaces`. The human-only `guild trust sign-plan` success hint now preserves the active non-default Guild root when it points operators at `guild trust verify-plan --plan ...`, so copy-paste review guidance no longer falls back to `~/.guild` and accidentally verifies against the wrong local trust store. Added a focused non-default-root regression to `crates/guild-mcp/tests/guild_cli.rs`, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli trust_sign_and_verify_plan_human_output_is_review_friendly -- --exact`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T19:09:05Z: Continued issue `#18` conformance closeout on branch `feat/18-trust-review-surfaces` after checking the epic against the live CLI. The remaining gaps were all still user-facing and intentionally narrow: `guild trust sign-plan` / `guild trust verify-plan` needed review-friendly human output instead of terse success lines, the shipped trust help/docs still needed explicit trust-store maintenance guidance (`add --record-file`, `list`, `remove`), and import/pull needed a more concrete installed-state verification explanation path that points operators at `guild verify -v`.

- 2026-03-23T19:11:02Z: Closed the remaining issue `#18` conformance gaps on branch `feat/18-trust-review-surfaces`. `guild trust sign-plan` and `guild trust verify-plan` now print compact review blocks with publisher, digest, trust tier, and clear next-step guidance; the real Clap-backed `guild trust --help` and subcommand `--help` surfaces now teach the review loop, trust-store maintenance flow, and plan-signing workflow; and README plus `docs/command-language.md` plus `docs/how-guild-works.md` now name `guild verify -v <skill-ref>` as the installed-state explanation path after import or pull. Focused validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T18:30:59Z: Started issue `#18` first implementation slice on branch `feat/18-trust-review-surfaces`. Scope is intentionally narrow and presentation-only: improve the human-readable trust review loop across `guild verify`, `guild trust list`, `guild trust add`, and successful `guild import` / `guild pull` using the existing host-owned trust metadata, while keeping registry behavior, signature verification semantics, and JSON / porcelain outputs unchanged.

- 2026-03-23T18:43:44Z: Completed issue `#18` first trust-review slice on branch `feat/18-trust-review-surfaces`. `guild verify` now renders one combined `status: <verification> / <trust>` line plus a publisher line, `guild trust list` and `guild trust add` now render review-friendly publisher summaries with actionable follow-up hints, and successful `guild import bundle`, `guild import oci-layout`, and `guild pull` now surface imported-skill trust review blocks plus the next likely verification step. Updated README, `docs/command-language.md`, `docs/how-guild-works.md`, and `guild help trust` to define the installed-state trust terms and the current review loop. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T18:18:00Z: Picked up a new PR `#35` review round on branch `feature/ux-20-actionable-errors`. The remaining trust-path gaps were narrow and classifier-focused: plain `guild trust verify-plan` argument mistakes were still being pulled into the trust/verification bucket via message matching, unsupported signed-plan signature formats needed to surface as `runtime/compatibility` instead of tamper guidance, and local trust-store filesystem failures from `guild trust list` / `guild trust remove` still needed to stay in `root/setup`. Updated the shared CLI classifier and next-step mapper for those cases, added focused CLI regressions for missing `--plan`, unsupported signed-plan format version, and trust-remove trust-store I/O failure guidance, and kept the follow-up command hints qualified to the selected registry root.

- 2026-03-23T16:52:00Z: Addressed all active PR `#35` review threads on branch `feature/ux-20-actionable-errors`. Follow-up stayed tight to the shipped CLI contract: runtime/policy recovery hints now shell-quote dynamic `skill://...` refs so copy-pasteable `guild show -v ...` follow-ups do not trigger shell redirection or globbing, persisted run-receipt `where:` lines now keep the durable `guild://executions/...` URI bare and reusable, and short evidence/object ref misses now classify as `resource/read` instead of falling through to `usage`. Added focused CLI regression coverage for quoted runtime follow-up refs and for missing evidence/object refs. Validation passed with `cargo fmt --all` and `cargo test -p guild-mcp --test guild_cli`.

- 2026-03-23T16:40:28Z: Completed the issue `#20` conformance follow-up on branch `feature/ux-20-actionable-errors`. Extended the shared CLI registry-error mapping so failed local source installs now print concrete recovery guidance and signed-bundle integrity failures during import now render as `trust/verification` with explicit retry/re-fetch advice instead of falling back to generic usage text. Added focused CLI regression coverage for failed `guild install`, failed `guild verify`, failed untrusted `guild import bundle`, failed tampered `guild import bundle`, and failed untrusted `guild pull`, and documented representative failure-path examples in README, `docs/command-language.md`, and `docs/testing.md`. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T16:34:42Z: Continued issue `#20` after an epic-conformance review on branch `feature/ux-20-actionable-errors`. The remaining gaps were narrower but real: failed `install` / `verify` / `import` / `pull` flows were not yet locked in the CLI regression suite, `docs/testing.md` still only showed success-oriented smoke paths, and some signed-bundle integrity failures still needed to present as trust/integrity problems rather than generic user mistakes on the human CLI path. This pass extends the shared CLI failure mapper with import/install integrity guidance, adds the missing regression coverage, and adds representative failure-path examples to the public docs and testing guide.

- 2026-03-23T16:10:30Z: Completed the first issue `#20` slice on branch `feature/ux-20-actionable-errors`. Added one shared human-facing CLI failure taxonomy in `crates/guild-mcp/src/cli.rs` without changing registry, runner, MCP, or JSON machine-output contracts; daily commands and `guild trust verify-plan` now render stable category labels plus actionable `reason`, `where`, and `Next:` guidance grounded in local Guild state. Added focused regression coverage for root/setup, lookup ambiguity, missing durable resources, policy denial, runtime-surface rejection, and tampered signed-plan failures in `crates/guild-mcp/tests/guild_cli.rs`, and documented the same user-facing labels in README and `docs/command-language.md`. Validation passed with `cargo fmt --all`, `git diff --check`, and `cargo test -p guild-mcp --test guild_cli`.

- 2026-03-23T15:49:32Z: Started issue `#20` on branch `feature/ux-20-actionable-errors`. Scope for this pass is intentionally narrow and CLI-facing: add one shared human-error taxonomy in `guild-mcp`, preserve existing registry/runner codes and persisted receipts, make the daily commands plus `guild trust verify-plan` print actionable follow-up guidance, and document the same failure labels in README / command-language docs / focused help text without widening JSON or MCP contract shape.

- 2026-03-23T15:15:44Z: Completed issue `#24` on branch `feature/ux-24-authority-lifecycle`. The branch stayed intentionally narrow and behavior-neutral: `guild run --help` now teaches the same four-stage declared/requested/granted/effective authority model as the main docs, README / `docs/command-language.md` / `docs/how-guild-works.md` now share one identical follow-up sentence about host-owned authority reduction, `SPECS.md` now carries a concise bridge from the normative policy record to that user-facing lifecycle vocabulary, and `examples/skills/hello-inspect/README.md` now ties `--grants-json` directly to requested authority in the smallest onboarding path. Added focused CLI/doc regression coverage in `crates/guild-mcp/tests/guild_cli.rs`. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T15:10:42Z: Started issue `#24` on branch `feature/ux-24-authority-lifecycle`. Repo-grounded gap check showed the primary docs already teach the four-stage authority model reasonably well, but the shipped `guild run --help` text still drifted with `runtime-effective authority` wording and `SPECS.md` did not yet carry a concise bridge to the same lifecycle labels. Scope for this branch is intentionally narrow: align the canonical declared/requested/granted/effective vocabulary across docs, help, spec language, and the smallest onboarding example without changing runtime or policy behavior.

- 2026-03-23T05:13:39Z: Started and completed the next Epic `#17` slice for issue `#29` on branch `feature/ux-23-identity-layers`. Added low-noise next-step hints only on the clearest successful human-default paths and kept machine surfaces unchanged: `guild install` now suggests `guild show -v <resolved-skill-ref>`, successful `guild run` status output now points to `guild why exec:...` plus `guild get guild://executions/...`, and human `guild why` output now points to `guild get <execution-uri>`. Rejected run output intentionally stays hint-free to avoid noisy or misleading advice on refusal paths. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T05:04:36Z: Started and completed the first Epic `#17` slice for issue `#28` on branch `feature/ux-23-identity-layers` on top of the local `#27` ref-resolution work. The shipped daily-verb help text now follows one tighter audit shape: scope, output expectations, accepted refs where relevant, compatibility-only alias notes, and one or two concrete follow-up help entries. `guild show`, `guild run`, `guild ls`, `guild get`, `guild why`, and `guild verify` now all expose consistent next-step guidance in real `--help`, and the no-arg manual usage printers reuse the same after-help blocks so CLI help does not drift by code path. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T04:54:54Z: Started and completed the next Epic `#16` follow-up slice for issue `#27` on branch `feature/ux-23-identity-layers`. Chosen implementation shape is intentionally narrow and grounded in the real resolver: `guild show -vv <skill-ref>` is now the first requested-ref explanation surface. The local registry gained one shared resolution-explanation helper that reuses the same installed-state selection logic as `resolve`, the human `guild show -vv` output now explains short-ref normalization, matching installed versions, highest-version selection, and why exactly one digest was selected, and README / command-language / daily-user docs plus shipped help text now define that surface explicitly. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T04:36:50Z: Addressed all currently open PR review threads on `feature/ux-23-identity-layers` for PR `#34`. The follow-up stayed intentionally small and user-facing: disabled blank GitHub issues in `.github/ISSUE_TEMPLATE/config.yml` so UX work routes through the new templates, added `guild ls` to the short daily-user command roles in `docs/how-guild-works.md`, aligned `guild help refs` with the exact identity-layer terminology already taught in README and the command-language guide (`source skill`, `installed executable state`, `resolved executable identity`), and strengthened the CLI regression suite so those help labels stay locked. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T03:15:06Z: Completed task `#25` on branch `feature/ux-23-identity-layers`. Tightened the public command-language story around the shipped CLI without widening runtime or contract shape: README now consistently frames the primary path as install/show/run/read-back, no longer summarizes the CLI with the older `show/list/read/explain` wording, and no longer uses the mixed "Install, Run, Inspect, Explain" quickstart heading; ADR `0019` now records the primary `show/run/ls/get/why/verify` surface with legacy aliases explicitly demoted to compatibility-only wording; the CLI regression suite now rejects markdown command examples that quietly teach `guild inspect`, `guild read`, `guild list`, or unshipped `guild explain`; and the real Clap `--help` surface for `guild ls`, `guild get`, and `guild why` now states their primary scope directly instead of relying on parse-error usage text. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`. Repo truth after this pass: Epic `#16`'s primary-command-language acceptance item is now explicitly covered on this branch alongside the earlier identity, capability-lifecycle, explainer, and follow-on-task work.

- 2026-03-23T03:00:19Z: Started task `#25` on branch `feature/ux-23-identity-layers` after the Epic `#16` conformance pass. Repo-grounded audit showed the remaining drift is now mostly wording rather than behavior: README still summarizes the CLI with the older `show/list/read/explain` nouns instead of the primary `show/run/ls/get/why/verify` story, ADR `0019` still uses older context wording around install/inspect/read in one historical setup paragraph, and the CLI regression suite does not yet protect against public markdown quietly teaching `guild inspect`, `guild read`, or `guild list` as command examples instead of compatibility aliases.

- 2026-03-23T03:03:41Z: Completed the follow-up Epic `#16` pass on branch `feature/ux-23-identity-layers`. Added the short daily-user explainer at `docs/how-guild-works.md`, linked it from README and `docs/command-language.md`, added one stable authority lifecycle (`declared` -> `requested` -> `granted` -> `effective at runtime`) to README, command-language docs, and shipped `guild run --help`, and tightened `guild show --help` so the non-mutating inspect surface stays explicit. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`. Repo truth after this pass: Epic `#16`'s previously missing capability-lifecycle and explainer acceptance items are now present on this branch.

- 2026-03-23T03:00:19Z: Started the next Epic `#16` follow-up pass on branch `feature/ux-23-identity-layers` after reviewing the epic against the live PR. Repo-grounded gap check showed the identity-layer acceptance item is now in place, but the epic is still short of full conformance because user-facing docs/help do not yet teach one stable capability lifecycle (`declared` -> `requested` -> `granted` -> `effective at runtime`) and there is still no short daily-user "How Guild works" explainer page discoverable from the main docs. Scope for this pass is intentionally narrow: add those missing user-facing surfaces without widening runtime or CLI contract shape.

- 2026-03-23T02:54:20Z: Completed UX task `#23` on branch `feature/ux-23-identity-layers`. The implementation stayed intentionally narrow and did not widen CLI or runtime contracts: README and `docs/command-language.md` now teach the three identity layers explicitly and point to `guild show -v` as the operator trace path, `guild help refs` and `guild show --help` now surface that same guidance in the shipped CLI, verbose `guild show` now labels the installed-state path honestly as `installed path:` instead of `source:`, and `crates/guild-mcp/tests/guild_cli.rs` now locks the new help text plus the requested/resolved/digest/installed-path verbose identity trace. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.

- 2026-03-23T02:51:00Z: Started implementation for UX task `#23` on branch `feature/ux-23-identity-layers`. Repo-grounded baseline before edits: the first active UX delivery slice is the mental-model and CLI-happy-path work under umbrella `#15`; the existing CLI already exposes requested-versus-resolved identity in `guild show --json` and `guild show -v`, but `guild help refs`, `guild show --help`, README, and command-language docs do not yet teach the three identity layers explicitly, and verbose `guild show` currently labels the installed path as `source:` which is misleading for the installed-state model.

- 2026-03-23T02:41:15Z: Finished the UX-hardening program reset scaffolding. Added plain markdown GitHub issue templates under `.github/ISSUE_TEMPLATE/`, created new active umbrella issue `#15` (`Guild UX hardening for real day-to-day use`), created UX epics `#16` through `#22`, added first-slice task issues `#23` through `#33` under the mental-model and CLI epics, and superseded the old patent umbrella `#1` with a forward link plus closed historical status. GitHub Projects v2 setup remains intentionally deferred because the current automation token still cannot access Projects, but the issue hierarchy is now live and usable.

- 2026-03-23T02:35:49Z: Started implementation of the UX-hardening program reset from `epics.md`. Repo-grounded baseline before edits: the live GitHub tracker is still centered on patent umbrella issue `#1`, there is no local `.github/ISSUE_TEMPLATE/` scaffolding, the current public CLI surface is already `show` / `run` / `ls` / `get` / `why` / `verify` with `inspect` / `read` / `list` as legacy aliases, and GitHub Projects v2 automation is currently blocked by token scope. Implementation scope for this pass is intentionally concrete: add local markdown issue templates, create a new UX umbrella with 7 child epics and first-slice tasks for mental-model plus CLI work, and supersede the old patent umbrella without rewriting the patent child issues or touching runtime contracts.

- 2026-03-23T02:00:28Z: Started the CLI-first docs/examples cleanup pass requested after the starter-pack work. Repo-grounded audit showed the main problem is not missing CLI surface but drift in markdown: README and command-language still mention `cargo run ... --bin guild -- ...`, many example skill READMEs still teach Cargo-wrapped `guild` commands, and `guild codex` helper output in `crates/guild-mcp/src/codex.rs` still prints Cargo-wrapped follow-up commands. Implementation scope is intentionally narrow: keep `cargo install --path crates/guild-mcp --bin guild` as the temporary install step, convert all post-install canonical workflows to `guild ...`, keep Cargo only for lower-level proof binaries and repo verification, and add a light regression check so user-facing markdown does not drift back to Cargo-wrapped `guild` usage.

- 2026-03-23T01:37:07Z: Follow-up tightening pass for the existing Guild Ops Starter Pack. Repo truth check showed the pack, core skills, secondary skills, and Codex smoke flows already exist and are wired through the real CLI/runtime path, so this pass is deliberately narrow: add the missing repo-level examples index at `examples/README.md`, tighten the starter-pack README around one numbered first-five-minutes flow, and add minimal discovery pointers from `README.md`, `docs/command-language.md`, and `docs/testing.md`. No runtime, contract, or control-plane semantics are being widened here.

- 2026-03-23T01:11:57Z: Follow-up pass for closeout and commit. Verified the workspace is still clippy-clean after the starter-pack and lint-fix work, then added one missing public-doc pointer in `docs/command-language.md` so the starter pack is discoverable from the main command-language/operator guide instead of only from README and testing docs. No runtime or contract semantics changed in this pass; it is docs-only on top of the already-validated implementation.
- 2026-03-23T01:09:07Z: Guild Ops Starter Pack implementation is complete and validated. Added the five new example skills plus shared helper under `examples/skills/`, added the pack entry README at `examples/skills/guild-ops-starter/README.md`, widened `guild codex` bootstrap/smoke coverage to install and exercise the new flows, added facade and CLI integration coverage for markdown-first output and exact single-child zero-authority rendering, and added minimal README/testing pointers for discovery. Validation status is clean: `cargo fmt --all`, `cargo fmt --all --check`, `cargo test -p guild-mcp --test inspect_facade --test guild_cli --test codex_workflow`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `git diff --check` all passed. Two small unrelated lint fixes were included so the repo-wide clippy gate would pass: lifetime elision in `crates/guild-draft-truth/src/patent_packet.rs` and a by-reference `run_help` helper in `crates/guild-mcp/src/cli.rs`.
- 2026-03-23T01:08:13Z: Focused and broad validation are almost complete. `cargo fmt --all`, `cargo test -p guild-mcp --test inspect_facade --test guild_cli --test codex_workflow`, and the full `cargo fmt --all --check && cargo test --workspace` pass all succeeded after the starter-pack changes. The remaining cleanup is a `cargo clippy --workspace --all-targets --all-features -- -D warnings` pass, which surfaced two small preexisting warning-level issues outside the pack logic itself: a needless lifetime in `crates/guild-draft-truth/src/patent_packet.rs` and a needless pass-by-value in `crates/guild-mcp/src/cli.rs`. Those are being cleaned up now so final validation matches the repo’s normal lint gate.
- 2026-03-22T23:51:16Z: Starter-pack implementation is now in the integration phase. The five new example skills plus the shared helper are on disk and all five install successfully into a real local Guild root (`render-report`, `incident-brief`, `run-diff`, `recent-failures`, `evidence-summary`). `crates/guild-mcp/src/codex.rs` has already been widened to bootstrap those skills and expose dedicated smoke flows for them, keeping the old explain/debug flows intact. Remaining work is repo-native validation and discoverability only: add targeted facade/CLI/Codex tests, add the pack README under `examples/skills/`, and add minimal README/testing pointers without changing runtime or contract surfaces.
- 2026-03-22T23:31:21Z: Started implementation for the Guild Ops Starter Pack. Repo-grounded packaging decision is to keep this as five new example skills under `examples/skills/` plus one pack README, with no new pack abstraction and no runtime/control-plane widening. Current execution/query/evidence truth confirmed before edits: `guild run` prints string payloads directly and object payloads as pretty JSON, bounded execution-query resources already expose status/policy/termination/evidence-count/child-count per match, evidence metadata is first-class under `guild://objects/records/{id}/metadata`, and the only honest composition slice remains exact single-child zero-authority invoke on `guild-skill-inspect-v1`. Implementation path is shared helper + `incident-brief` / `run-diff` / `recent-failures` / `evidence-summary` / `render-report`, then pack docs, Codex smoke wiring, targeted tests, and final validation.
- 2026-03-22T23:09:33Z: Finished the CLI help and user-facing docs refresh. `guild --help` is now a short grouped landing page, `guild help refs|trust|roots` is a real topic surface, `show`/`run`/`why`/`verify` help now points to targeted follow-up guidance, and `guild run --help` now advertises the real `--input-file` flag instead of the stale `--input-file-path` struct name. Rewrote the README, command-language guide, roadmap, ADR index, and CLI ADR around a product-first/operator-first voice, plus light summary-language cleanup in `SPECS.md`, `ARCHITECTURE.md`, and `docs/testing.md`. Validation passed with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli --test codex_workflow --test mcp_server_stdio`, and `git diff --check`.
- 2026-03-22T22:49:42Z: Started the CLI help and user-facing docs refresh. Repo-grounded baseline before edits: `guild --help` still uses flat Clap output with an overloaded footer, there is no real `guild help <topic>` surface, `guild run --help` currently advertises the wrong `--input-file-path` flag, `guild codex --help` and several docs still say `dogfood`, and the README plus command-language docs still lead with milestone/internal framing more than operator tasks. Implementation scope for this pass is the real shipped help surface, README/command-language/roadmap/ADR-index/ADR-0019 cleanup, and summary-language alignment in `SPECS.md` and `ARCHITECTURE.md`, followed by focused CLI help regression coverage and one final commit.
- 2026-03-22T22:32:38Z: Finished the `M9-patent-packet-from-measured-behavior` implementation pass. Added a measured technical packet under `docs/patent/` with an invention brief, technical-delta memo, prior-art/delta matrix, claim ladder, evidence map, figure source set, non-claims memo, M10 next-step note, and a machine-readable `m9-packet-manifest.json` keyed to real benchmark slice ids, support-matrix entries, test names, scenario names, and codepaths. Added a separate Rust-native `patent_packet` checker under `crates/guild-draft-truth` plus `xtask` command `patent-packet check`, kept the packet downstream from the draft-v1 truth bundle, and lightly updated README/testing pointers. Validation passed with `cargo fmt --all`, `cargo test -p guild-draft-truth`, `target/debug/xtask draft-v1 truth check`, `target/debug/xtask patent-packet check`, and `git diff --check`.
- 2026-03-22T21:57:08Z: Started implementation for `M9-patent-packet-from-measured-behavior`. Repo-grounded baseline before edits: no existing `docs/patent/` area exists, Mermaid-in-Markdown is the only in-repo figure-source convention, `docs/schemas/draft-v1/family_support_matrix.json` and `docs/schemas/draft-v1/benchmark_matrix.json` already carry the measured support frontier and fail-closed walls as machine-readable truth, and `xtask` currently validates only the draft-v1 truth bundle. Implementation path is to add a measured packet under `docs/patent/`, back it with a minimal machine-readable manifest keyed by real slice and wall ids, add a separate repo-native `xtask` checker for packet consistency and overclaim prevention, and keep top-level doc changes limited to pointers and validation commands rather than another broad docs rewrite.
- 2026-03-22T22:05:34Z: Finished the `M8f-cli-ux-contract-aware` implementation pass. `guild-mcp` now has a typed Clap front-end for the primary and legacy command tree instead of the old manual top-level dispatcher, the shared presenter now uses Rust-owned status labels instead of emitting the non-canonical `ok` synonym, `guild run` now emits the same stderr status summary on persisted refusal paths before returning the fail-closed error, and the docs/example drift for `inspect`/`read` happy paths was tightened (`hello-inspect`, `summarize-execution-query`, ADR 0019 alias note). Added CLI regression coverage for ambiguity failures, color/`NO_COLOR` behavior, porcelain color-independence, and refusal stdout/stderr behavior. Validation status: `cargo fmt --all`, `cargo check -p guild-mcp`, `cargo test -p guild-types`, `cargo test -p guild-mcp --test guild_cli` passed before the final clippy-only format-string cleanup, and `cargo clippy -p guild-mcp -p guild-types --all-targets --all-features -- -D warnings` passed. Full `cargo test --workspace` and a later repeat `cargo test -p guild-mcp --test guild_cli` both hit environment-level linker `Bus error` failures while linking large unchanged binaries/examples, so those runs were not reliable semantic signal on the source edits themselves.
- 2026-03-22T20:32:32Z: Started implementation for `M8f-cli-ux-contract-aware`. Repo-backed baseline before edits: `crates/guild-mcp/src/cli.rs` still uses a manual top-level dispatcher and per-command flag scanning even though the docs and `guild_cli` integration suite already teach the intended `show` / `run` / `ls` / `get` / `why` / `verify` surface. Current `cli_presenter.rs` already centralizes much of the text rendering, but it still duplicates some status labels locally and still emits the non-frozen `ok` run status. Implementation path is to put a real Clap tree in front of the existing substrate-backed handlers, reuse the current registry/runtime behavior rather than redefining it, collapse human and porcelain wording onto shared summary builders plus frozen vocabulary helpers, preserve the shipped legacy aliases (`inspect`, `read`, `list`) without adding unshipped ones (`apply`, `explain`), and then tighten tests/docs around ambiguity failures, color behavior, stdout/stderr separation, and the new help surface.
- 2026-03-22T19:08:18Z: Finished validation on the `M8e-contract-surface-v1-core` tree. `cargo fmt --all --check`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo run -q -p xtask -- draft-v1 truth check`, and `git diff --check` all pass. The new exact `SPECS.md` marker blocks for URI roots, active families, status vocabulary, and executable identity now validate against shared Rust-owned contract-surface constants and the live runner family set. The draft-v1 checked artifacts under `docs/schemas/draft-v1/` and the benchmark report directory remained byte-stable in this pass; no generated support, compatibility, or benchmark outputs changed.
- 2026-03-22T18:28:34Z: Started implementation for `M8e-contract-surface-v1-core`. Landed the first hardening pass across code and docs: `SPECS.md` now carries an explicit Contract Surface v1 (core) freeze with exact marker blocks for URI roots, active live family registry, status vocabulary, and executable identity terms; `guild-types` now owns shared contract-surface constants for canonical URI/query roots, query-status segments, identity-field names, and support/linkage spellings; `guild-draft-truth` now checks those exact `SPECS.md` marker blocks fail-closed instead of trusting prose; and the CLI presenter now reads shared support-vocabulary constants instead of hardcoding its own parallel words. Explanatory docs and the relevant ADRs were also retargeted back to `SPECS.md` so they stop competing for normative ownership. Next step is formatting and regression validation, then tighten any compile or truth-check fallout.
- 2026-03-22T17:21:03Z: Finished validation on the final `M8d-truth-surface-consolidation` tree. `cargo fmt --all --check`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo run -q -p xtask -- draft-v1 truth check` all pass. The Rust-native truth gate confirms wrapper removal plus source-of-truth/doc-marker checks, support-matrix and benchmark generators still match the checked artifacts exactly, and no generated JSON or Markdown artifact content changed in this pass. Follow-on cleanup needed for the repo-wide enforced path ended up including a handful of narrow clippy fixes in `guild-runner`, `guild-mcp`, and `xtask`, plus formatting-only updates in `crates/guild-runner/examples/live_proof_scenarios.rs` that were already outstanding in the repo-wide fmt gate.
- 2026-03-22T16:04:19Z: Landed the consolidation edits for `M8d-truth-surface-consolidation`. The shared Rust truth layer now carries canonical support-status, linkage-status, and linked-path vocabulary plus runner-backed active-family and canonical Guild URI-root checks; `support_matrix.rs` now generates from canonical family ids and validates its own family/status/root vocabulary; `benchmark.rs` now replaces the large token-issuance tuple with a typed struct and adds explicit vocabulary checks for support status, token linkage, witness linkage, linked path, and reduction classification. Repo-visible source-of-truth ownership is now explicit in `SPECS.md`, the root `ARCHITECTURE.md`, README/testing/draft-v1 docs, compatibility-wrapper docs, and ADR surfaces. The obsolete Python truth entrypoints under `docs/schemas/draft-v1/` were removed, `Makefile` now exposes `fmt-check` plus `verify`, and a checked GitHub Actions workflow now runs fmt/test/clippy/truth-check. Next step is full validation and artifact parity confirmation.
- 2026-03-22T15:42:59Z: Started `M8d-truth-surface-consolidation` implementation. Repo-grounded truth before edits: the Rust-native truth path already exists under `xtask` plus `crates/guild-draft-truth`, the three requested Python truth entrypoints are already only deprecation wrappers, `cargo run -q -p xtask -- draft-v1 truth check` passes on the current tree, and the main remaining drift risks are explicit source-of-truth ownership, duplicated family/URI/status strings across docs and generators, wrapper/docs/ADR surfaces that still restate live contract truth too aggressively, and the missing checked-in CI workflow. Implementation will stay narrow: make normative ownership explicit in `SPECS.md`, thin wrapper docs and ADRs back to rationale/pointer surfaces, add one shared Rust truth vocabulary/drift-check layer, remove the obsolete Python truth wrappers, and make the standard Rust-native verify path enforceable with docs/Makefile/CI.
- 2026-03-22T13:48:38Z: Started a real direct-dependency upgrade attempt after dry-running `cargo upgrade -n --incompatible`. Current direct upgrade surface is: workspace `rand_core` `0.6 -> 0.10`, `wasmtime` / `wasmtime-wasi` / `wasmtime-wasi-http` `42.0.1 -> 43.0.0`, `guild-mcp` `toml_edit` `0.22 -> 0.25`, plus newer direct deps available in the standalone example `skill-rust` crates (`serde_json`, `url`, `wit-bindgen`). Plan is to upgrade manifest requirements first, then let compile/test failures tell us which breakages are real API migrations rather than speculative fear.
- 2026-03-22T07:05:00Z: Started a docs sync pass after the repo-owned Cargo package version bump and regression sweep. Narrow docs goal: make the version story explicit without lying about the checked-in example skills, so the canonical docs now distinguish workspace Cargo package version `0.1.1` from example Guild skill manifest version `0.1.0` / `@^0.1`, and the verification docs now point at the non-mutating `cargo fmt --all --check` gate while leaving `cargo fmt --all` as the rewrite command.
- 2026-03-22T06:57:51Z: Finished the full regression sweep after the version bump. Results: `cargo test --workspace` passed end-to-end across workspace crates, examples, integration tests, and doc tests; `cargo run -q -p xtask -- draft-v1 truth check` passed with clean support-matrix, compatibility-matrix, benchmark-artifact, and report validation; `cargo fmt --all --check` failed on formatting drift in `crates/guild-runner/examples/live_proof_scenarios.rs`; and `cargo clippy --workspace --all-targets --all-features -- -D warnings` failed on one concrete lint in `crates/guild-draft-truth/src/benchmark.rs` where the return tuple at line 936 triggers `clippy::type_complexity` under the repo's `-D warnings` policy. No behavioral test regressions surfaced in the documented workspace sweep.
- 2026-03-22T06:18:03Z: Started a full regression sweep after the repo-owned Cargo package version bump. Following the documented broad verification path from `docs/testing.md`: check formatting drift, run `cargo test --workspace`, run `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and run the Rust-native draft-v1 truth gate with `cargo run -q -p xtask -- draft-v1 truth check`, then summarize any regressions or drift instead of assuming the version-only change is risk-free.
- 2026-03-22T06:13:37Z: Started a repo-owned crate version bump after confirming the workspace crates already inherit one shared root version and the example `skill-rust` packages are standalone Cargo crates outside that workspace. Taking the narrow path: bump Cargo package versions only from `0.1.0` to `0.1.1` across the workspace root plus example Rust crates, leave Guild skill manifest/runtime versions alone for now, then verify with cargo metadata/build checks so the change stays package-scoped rather than silently changing execution contracts.
- 2026-03-22T04:29:40Z: Started `M8e-cli-ux-polish`. Repo-grounded truth before edits: the shipped `guild` CLI still lives entirely in `crates/guild-mcp/src/cli.rs`, uses a manual parser plus direct `println!` formatting, exposes `inspect`/`read`/`list` as the primary UX, and has no shared presenter, no color policy, no verbosity model, and no porcelain mode. Implementation is intentionally CLI-local and substrate-backed: promote `show`/`run`/`ls`/`get`/`why`/`verify`, keep `inspect` as a legacy execution alias to `run`, keep `verify` scoped to installed skill refs only, add thin read-only listing helpers for `ls objects|evidence`, preserve existing machine-readable JSON shapes where that keeps compatibility honest, and make default human output compact with explicit `proof-backed` / `bounded` / `not_proven` / `refused` wording instead of decorative noise.
- 2026-03-21T23:51:00Z: Started `M8-proper-real-path-benchmarking`. Repo-grounded truth before edits: the live Rust runner already exposes deterministic supported and unsupported live-proof scenarios for `read-resource`, six bounded `http-request` slices, one bounded `invoke-skill` slice, exact `log-write`, and explicit fail-closed `emit-evidence` replay-unavailable behavior, while the draft-v1 validator already shells into those scenarios to prove plan -> proof -> token -> witness linkage for some but not all supported slices. Implementation will stay narrow and honest: add only the missing benchmarked scenario coverage, add a slice-aware benchmark harness and schema under `docs/schemas/draft-v1/`, generate machine-readable and human-readable benchmark artifacts from repo-backed scenarios, and update docs/support matrices so supported, fallback, refusal, unlinked, and `not_proven` paths remain explicit instead of averaged away.
- 2026-03-22T00:32:00Z: Progressed `M8-proper-real-path-benchmarking` into the real measurement path. The live-proof scenario example now has reusable prepared-scenario helpers plus a `benchmark` subcommand, `docs/schemas/draft-v1/benchmark_real_path.py` now measures M4 admission plus M5 live proof plus M6 or M7 linkage or fallback behavior slice-by-slice, `docs/schemas/draft-v1/benchmark_matrix.schema.json` now validates the machine-readable artifact shape, and `validate_examples.py` now treats benchmark artifact validation as part of the checked draft-v1 bundle. Focused smoke checks already confirmed the three real benchmark modes: supported `read-resource` is 10/10 proof-backed issuance and 10/10 proof-linked witness generation on the measured immutable-root slice, unsupported redirect `http-request` stays `not_proven` with 10/10 default refusal plus 10/10 explicit upper-bound fallback and 10/10 unlinked witness generation, and `log-write` is measured as M4 plus M5 only with no synthetic M6 or M7 success path. The full matrix generation is still running against the repo-backed scenarios so the final docs pass can cite measured results instead of assumptions.
- 2026-03-22T01:45:00Z: Finished `M8-proper-real-path-benchmarking` implementation and focused validation. Generated `docs/schemas/draft-v1/benchmark_matrix.json` plus `docs/benchmarking/m8-real-path-benchmark.md`, updated the support matrix and top docs to point at the benchmark artifact and to state the exact measured slice set, and kept `log-write` honest as a proof-only benchmarked slice rather than inventing checked M6/M7 linkage. Final measured benchmark truth on the checked path: supported proof-linked slices are one bounded `read-resource` immutable-root slice, six bounded `http-request` replay slices, and one bounded `invoke-skill` single-child zero-authority slice; supported proof-only slice is one exact `log-write` observed `info`-level slice; unsupported benchmarked slices are redirect `http-request`, multi-child `invoke-skill`, and replay-unavailable `emit-evidence`, each with `10/10` default refusal, `10/10` explicit upper-bound fallback, and `10/10` unlinked witness generation; extra fail-closed walls for `http-request` no-replay, `read-resource` execution-query shrink, and `invoke-skill` child-authority use each trigger `10/10`. Focused verification passed with `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/benchmark_real_path.py --check-artifacts`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/compatibility_check.py`, `cargo test -p guild-runner --test live_proofs --test http_requests --test composition --test inspect_slice`, and `git diff --check`.
- 2026-03-21T23:24:10Z: Landed the narrow `emit-evidence` feasibility result on the fail-closed branch. Runtime-side changes now persist and observe a host-owned `EvidenceSinkDescriptor` for the fixed local object-store path, and the live proof path now has a dedicated single-sink comparator that binds sink identity plus payload digest, normalizes only host-minted record handles, and includes an adversarial comparator test where parent success stays the same but sink identity changes. The milestone outcome remains explicitly `not_proven`: one exact single-emission local-object-store replay/comparator slice can be checked inside the runtime, but the current canonical `emit-evidence` authority shape and draft-v1 control-plane still do not model exact sink and payload identity as first-class authority, so plan -> proof -> token and proof -> witness linkage stay unavailable with stable `EMIT_EVIDENCE_*` reason codes and updated support-matrix/docs truth.
- 2026-03-21T22:52:07Z: Started `M8c-emit-evidence-single-sink-feasibility`. Repo-grounded truth before edits: the live runtime already authorizes and persists `emit-evidence` through one local object-store path, but `crates/guild-runner/src/live_proof.rs` still hard-codes the family to `not_proven`, and draft-v1 currently models canonical `emit-evidence` only as audience/redaction plus byte ceiling. Implementation scope is intentionally narrow and fail-closed: bind sink identity explicitly as host-owned truth, attempt exactly one single-emission local-object-store replay/comparator slice, normalize only truly host-minted fields, add an adversarial comparator test, and keep plan->proof->token or proof->witness linkage unavailable if draft-v1 cannot represent the exact sink/payload slice honestly.
- 2026-03-21T22:21:12Z: Finished a docs truth pass after the M8c invoke landing. Tightened `README.md`, `ARCHITECTURE.md`, `docs/testing.md`, `docs/schemas/draft-v1/README.md`, and `docs/schemas/draft-v1/family_support_matrix.json` so every summary of the supported `invoke-skill` slice now includes the real zero-nested-child-executions boundary rather than implying a broader exact-single-child proof envelope. Also updated the example READMEs so `invoke-parent-single-child` and `invoke-child-zero` are documented as the dedicated proof fixtures, while `hello-composite` and `hello-inspect` now say plainly that they are real runtime examples but not the proof-backed invoke slice because that path still exercises `emit-evidence`.
- 2026-03-21T20:30:06Z: Finished `M8c-http-localhost-head-explicit`. The bounded live-proof-backed `http-request` envelope now includes one additional hostname slice beyond the prior five: explicit-port `localhost` `HEAD` over `http`, exact observed path only, no query or fragment, no redirects, single exercised request, normalized inspect-output comparator only, with deterministic replay fixtures and live observations both requiring a host-owned loopback-only resolution binding over the literal host string, explicit port, resolved addresses, and address families. Replay catalog validation, replay transport admission, bounded live-proof classification, draft-v1 support truth, example contracts/requests/execution records, and the checked plan -> proof -> token -> witness linkage were all updated together while keeping `localhost` default-port `GET`, `localhost` default-port `HEAD`, other hostname forms, redirects, query/fragment components, multi-request executions, `https`, `invoke-skill`, and `emit-evidence` explicitly `not_proven`. Validation passed with `cargo fmt --all`, `python3 -m py_compile docs/schemas/draft-v1/validate_examples.py`, `cargo test -q -p guild-runner --test http_requests -- --nocapture`, `cargo test -q -p guild-runner --test live_proofs -- --nocapture`, `cargo run -q -p guild-runner --example live_proof_scenarios -- http-request-localhost-head-bounded`, `/tmp/guild-schema-venv-m8c-head/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv-m8c-head/bin/python docs/schemas/draft-v1/compatibility_check.py`, and `git diff --check`.
- 2026-03-21T20:12:57Z: Started `M8c-http-localhost-head-explicit` after repo-grounded planning and static/runtime verification. Current repo truth is: the live host path already supports `localhost` resolution bindings and bodyless `HEAD` execution independently, while proof-only replay catalog validation, replay transport admission, and bounded live-proof classification still hard-stop `localhost` hostname replay at explicit-port `GET` only. Implementation scope is intentionally narrow: attempt exactly one additional resolver-bound slice, explicit-port `localhost` `HEAD` over `http`, with one exercised request, exact observed path, no query or fragment, no redirects, normalized inspect-output comparator only, deterministic replay fixture required, and existing plan -> proof -> token -> witness linkage reused only if the live proof record stays honest; otherwise keep the slice explicitly `not_proven`.
- 2026-03-21T15:05:30Z: Finished the resolver-binding hostname pass. The repo now has one additional bounded live-proof-backed `http-request` slice beyond the existing four loopback-IP slices: explicit-port `localhost` `GET` over `http`, exact observed path only, no query, no redirects, single exercised request, normalized inspect-output comparator only, with a deterministic replay fixture that binds the literal host string, explicit port, resolved address plus address family, loopback-only resolution semantics, and replay/proof invalidation inputs. Runtime observation records now persist host-owned HTTP resolution bindings, proof-only replay and live-proof search consume that binding without ambient DNS assumptions, proof-backed token issuance and proof-linked witnesses now extend honestly to this exact slice only, and draft-v1 support truth/examples/docs were updated to name the five-slice set while keeping `localhost` default-port `GET`, `localhost` `HEAD`, other hostname forms, query, redirects, multi-request executions, and `https` explicitly `not_proven`. Validation passed with full `cargo test -p guild-runner --test http_requests -- --nocapture`, full `cargo test -p guild-runner --test live_proofs -- --nocapture`, `/tmp/guild-schema-venv-m8c-head/bin/python docs/schemas/draft-v1/validate_examples.py`, and `/tmp/guild-schema-venv-m8c-head/bin/python docs/schemas/draft-v1/compatibility_check.py`.
- 2026-03-21T14:28:40Z: Started the hostname resolver-binding follow-on after confirming the repo already has four proven loopback-IP HTTP slices (`GET` and `HEAD`, each explicit-port and implicit-default-port) and the next approved experiment is narrower host-form expansion only. Implementation target is one additional explicit-port `localhost` `GET` slice if and only if the proof path can bind host string, explicit port, resolved loopback addresses plus address family, and replay/proof invalidation inputs without ambient DNS assumptions; otherwise the pass must fail closed with explicit `not_proven` reasons. Planned code surface is host-owned HTTP observation data, proof-only replay fixtures plus replay-time authorization, bounded live-proof classification, focused Rust coverage, checked draft-v1 runtime-alignment examples, and docs/support-matrix truth updates.
- 2026-03-21T13:55:05Z: Added a new local-only `findings.md` handoff note at the repo root after the `269c6de` push, per user request. The note summarizes current milestone status, the exact live-proof envelope now implemented for `read-resource`, `log-write`, and `http-request`, the remaining explicit `not_proven` boundaries, validation already run on the pushed tree, and the next honest work sequence. It is intentionally left out of git history.
- 2026-03-21T05:43:14Z: Finished the approved `M8c-http-next-slice` pass. The next honest slice was a method-only expansion to replay-fixtured loopback-IP `HEAD` over `http` under the same single-request, exact-path, no-query, no-redirect, normalized-inspect comparator boundary already used by `GET`. Landed proof-only replay support for `HEAD` in `crates/guild-runner/src/lib.rs` with an explicit fail-closed invariant that `HEAD` replay fixtures must carry an empty response body, widened live-proof classification in `crates/guild-runner/src/live_proof.rs`, added focused Rust coverage in `crates/guild-runner/tests/http_requests.rs` and `crates/guild-runner/tests/live_proofs.rs`, and added real runner-backed scenarios in `crates/guild-runner/examples/live_proof_scenarios.rs`. The machine-readable and docs truth now name four proven `http-request` slices total: `GET` explicit-port, `GET` implicit-default-port, `HEAD` explicit-port, and `HEAD` implicit-default-port; hostname/query/redirect/multi-request/https shapes still remain explicitly `not_proven`. Added checked draft-v1 runtime-alignment fixtures for `runtime-http-head` and `runtime-http-head-default-port`, regenerated `docs/schemas/draft-v1/compatibility_matrix.md`, and updated README/SPECS/ARCHITECTURE/testing/docs to state the exact bounded slice set and downstream proof-backed token or witness linkage truth. Validation passed with `cargo test -p guild-runner --test live_proofs -- --nocapture`, `cargo test -p guild-runner --test http_requests -- --nocapture`, `cargo fmt --all`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/compatibility_check.py`, and `git diff --check`.
- 2026-03-21T05:23:47Z: Began the approved `M8c-http-next-slice` implementation pass. Repo truth now says the already-proven HTTP live-proof envelope includes two `GET` slices, explicit-port and implicit-default-port loopback IP over `http`; the next honest candidate is a method-only expansion to replay-fixtured `HEAD` under the same single-request, no-query, no-redirect, normalized-inspect comparator boundary. Planned changes are intentionally narrow: extend proof-only replay and live-proof classification for `HEAD`, add real Rust proof and transport coverage plus one checked draft-v1 runtime-alignment case, keep broader hostname/query/redirect/multi-request/https shapes explicitly `not_proven`, and update support truth/docs to name the exact proven slice inventory after the patch.
- 2026-03-21T05:32:00Z: Added a new uncommitted `findings.md` handoff note at the repo root after pushing `dede400`. The note captures the current repo-backed milestone snapshot, the exact live-proof envelope now implemented for `read-resource`, `log-write`, and `http-request`, the concrete M8c-http-next-slice work that landed, the validations that were run, the remaining gaps, and the next honest work sequence. It is intentionally left out of git history per user request.
- 2026-03-21T05:18:00Z: Started a GitHub tracking sync after landing the M8c implicit-default-port HTTP slice. The stale tracker state is in GitHub, not the repo: milestone `M8c` and epics `#14` and `#1` still describe `http-request` live proof as only the original explicit-port loopback `GET` slice. The update target is narrow and status-preserving: keep M8c open/partial, but rewrite the milestone and epic text to name the exact two proven slices, keep `invoke-skill` and `emit-evidence` as `not_proven`, and keep broader HTTP shapes explicitly outside the live-proof envelope.
- 2026-03-21T05:06:00Z: Validation found one typed-schema integration bug for the new M8c implicit-default-port HTTP slice: the live runner emits `replay_input_digest` as a prefixed digest string, while draft-v1 proof records use structured digest objects. Fixed `docs/schemas/draft-v1/validate_examples.py` so live proof records and cache key material parse that replay digest into the same structured digest shape used everywhere else, preserving typed draft truth rather than widening the proof schema toward stringly fields. Full Rust `http_requests` and `live_proofs` suites were already green before this rerun; remaining step is rerunning `validate_examples.py` and confirming the regenerated compatibility matrix stays aligned.
- 2026-03-21T04:39:00Z: Started M8c-http-next-slice implementation after approval. Repo truth from the live proof/runtime code says the narrowest honest next `http-request` slice is not a new transport behavior but the already-normalized implicit default-port shape: `GET http://127.0.0.1/response.json` with grant port `80`, one exercised request, no query, no redirects, replay fixture required, normalized inspect-output comparator only. Implementation scope is therefore truth-alignment plus proof coverage: add Rust replay/live-proof tests and scenario output for the implicit-default-port slice, carry a stable replay-input digest into the live proof output and draft proof-record cache key material, add draft-v1 contract/request/invocation/execution-record examples for the default-port slice, and update support matrices/docs to name the exact supported slice set while keeping hostname/query/redirect/multi-request/https shapes explicitly `not_proven`.
- 2026-03-21T04:12:28Z: Started a new uncommitted `findings.md` handoff note at user request. Scope is repo-summary only: capture the current snapshot at `cfc889e`, summarize what has landed from M3 through partial M8c, name the exact supported live-proof envelope and current operational/tooling state, call out the real remaining gaps, recommend the next honest work, and leave the file uncommitted by design.
- 2026-03-21T04:10:33Z: Finished the GitHub tracking sync. Updated milestone objects so M3-M6 are now closed as completed bounded draft-v1 milestones, refreshed M8a to point at M8b/M8c as the follow-on truth, created closed milestone/epic `M8b Direct Canonical Family Support in Draft-v1`, created open milestone/epic `M8c Live Proof Basis and Honest End-to-End Linkage`, and refreshed M9/M10 milestone language to the current repo-backed prereq state. Closed epic issues #5-#8, updated epic bodies for M8a/M9/M10, created #13 for M8b and #14 for M8c, rewrote umbrella issue #1 to treat M8c as the current open bridge milestone, and added #13 and #14 as real sub-issues under #1. Verification via `gh issue list --state all --json ...`, `gh api repos/jkordish/Guild/milestones?state=all`, and `github.issue_read(get_sub_issues)` now shows the expected milestone and epic topology.
- 2026-03-21T04:04:58Z: Started a GitHub tracking sync pass at user request. Current repo-backed truth from `README.md` is: M3-M7 complete as bounded draft-v1 milestones, M8a complete, M8b complete, M8c partial, M0 and M2 partial, M1/M9/M10 not started. GitHub was stale in two directions: M3-M6 were still open as "implemented, still open", and there were no milestone or epic objects yet for M8b or M8c. The update plan is to bring milestone descriptions, epic states, and the umbrella tracker into line with the repo-backed status without inventing broader support claims.
- 2026-03-21T04:03:25Z: Started a README language-tightening pass at user request. Scope is README-only and milestone-driven: rewrite the opening/status/current-state language so it reads directly off M3 through M10, trims older rhetorical phrasing, and states the current M8c boundary exactly without changing any code or support claims.
- 2026-03-21T04:01:13Z: Clippy pass narrowed to three mechanical follow-ups in `crates/guild-runner/src/live_proof.rs` after the first fix round: `comparator_descriptor` still took a trivially copyable comparator profile by reference, and two `clone()` calls remained on the now-`Copy` comparator profile. Patched those without changing any proof semantics and reran the full `cargo fmt --all && cargo clippy --workspace --all-targets --all-features -- -D warnings` sweep.
- 2026-03-21T04:05:00Z: Started a focused clippy-fix pass after the M8c-http implementation. Goal is narrow and mechanical: run the strict clippy sweep, fix only the reported lint failures, rerun the same checks, and avoid changing the newly bounded HTTP proof semantics unless a lint forces a small refactor.
- 2026-03-21T03:55:00Z: Finished M8c-http. Added a proof-only deterministic HTTP replay seam in `crates/guild-runner/src/lib.rs`, bounded `http-request` live proof search in `crates/guild-runner/src/live_proof.rs`, focused Rust coverage in `crates/guild-runner/tests/live_proofs.rs` and `crates/guild-runner/tests/http_requests.rs`, and live-proof scenario outputs for the supported slice plus redirect/no-replay fail-closed cases. The supported live-proof envelope is deliberately narrow and now documented machine-readably across draft-v1: exactly one exercised replay-fixtured loopback-IP `GET` over `http`, explicit port, exact observed path-prefix, no query, no redirects, normalized inspect-output comparator only, bounded status only, and honest plan -> proof -> token -> witness linkage for that slice. Broader `http-request` shapes still stay `not_proven`, token issuance falls back to upper-bound-only when allowed, and witness linkage stays unavailable outside the proven replay slice. Validation passed with `cargo test -p guild-runner --test live_proofs -- --nocapture`, `cargo test -p guild-runner --test http_requests -- --nocapture`, `.venv/bin/python docs/schemas/draft-v1/validate_examples.py`, and `.venv/bin/python docs/schemas/draft-v1/compatibility_check.py`.
- 2026-03-21T03:05:00Z: Started M8c-http implementation after a repo-grounded plan and user approval. The clean injection seam is `RunnerRuntimeHost::http_request` in `crates/guild-runner/src/lib.rs`: every Wasm guest HTTP call already flows through that host boundary, so the first honest replay transport can stay proof-only and host-owned without contaminating normal runtime execution. Current implementation target is deliberately narrow and fail-closed: one exercised loopback-IP `GET` over `http`, explicit port, exact path, no query, no redirects, deterministic fixture-backed replay, normalized inspect-output comparator only, bounded live proof status rather than exact minimality, and explicit `not_proven` fallback for everything outside that slice.
- 2026-03-21T02:35:03Z: Added a local-only `findings.md` handoff summary after the M8c push. The file captures the current repo snapshot at `fb595fb`, states the actual milestone status from M3 through M10, calls out the checked M8c truth (`read-resource` bounded live proof with an end-to-end chain, `log-write` live family proof, `http-request`/`invoke-skill`/`emit-evidence` still `not_proven`), and lists the next honest blockers without committing the note into git history.
- 2026-03-21T02:25:37Z: Started a README language-tightening follow-up. Scope is intentionally small and milestone-driven: keep the operator-facing command guidance intact, but rewrite the top-level status, milestone summary, and current-boundary language so the README reads directly off M3 through M8c instead of older M8a-era phrasing. The key doc truth being carried forward is that M8c is only partially complete: `read-resource` has bounded live proof with a checked plan -> proof -> token -> witness chain, `log-write` has real live family proof, and `http-request`, `invoke-skill`, and `emit-evidence` remain explicitly `not_proven`.
- 2026-03-21T02:34:00Z: Finished M8c live proof basis and honest end-to-end linkage. Added a real Rust live-proof engine in `crates/guild-runner/src/live_proof.rs` plus `Runner::prove_live_authority`, real integration coverage in `crates/guild-runner/tests/live_proofs.rs`, and a reusable `live_proof_scenarios` example that emits real runner-backed proof envelopes. Current honest support is asymmetric by design: `read-resource` now has bounded live proof over immutable `guild://executions/` and `guild://objects/records/` roots and a checked plan -> proof -> token -> witness chain; `log-write` now has real live family proof over the observed discrete log-level slice; `http-request`, `invoke-skill`, and `emit-evidence` still remain `not_proven` with explicit reason codes. Updated the draft-v1 schemas, token/witness linkage logic, validator, support matrix, and docs so live-runtime proof is distinguished from draft-example proof, proof-backed issuance can require live proof where requested, and witnesses stay unlinked when proof is absent or not acceptable. Validation passed with `cargo test -p guild-runner --test live_proofs -- --nocapture`, `cargo test -p guild-runner --test http_requests --test resource_reads --test composition --test inspect_slice -- --nocapture`, and `/tmp/guild-schema-venv-m8c/bin/python docs/schemas/draft-v1/validate_examples.py`.
- 2026-03-20T20:05:00Z: Started M8a runtime alignment and canonical effect vocabulary implementation after inventorying both sides. Current repo truth is that the live Rust capability family surface is already typed and narrower than the draft-v1 `effect_class` bundle, while durable execution records still lack a first-class exercised-versus-blocked authority event stream. Implementation plan is the conservative bridge: add host-owned canonical authority observations to live Rust execution records for the active inspect families, keep unsupported or unmappable families fail-closed, add a draft-v1 live-runtime mapping layer that consumes those canonical records with explicit exact/narrowing/partial/unsupported classification plus complete/partial/unavailable coverage semantics, and update docs/examples/tests to say exactly which families are now runtime-general and which remain blocked.
- 2026-03-20T16:47:02Z: Finished the draft-bundle M6 token implementation on `docs/schemas/draft-v1/`. Added `delegated_capability_token.schema.json` plus `token_verification_result.schema.json`, widened `common.schema.json` with stable issuance and verification reason-code vocabulary, added `token_core.py` and `token_engine.py` for fail-closed root issuance, delegated child issuance, and verification using canonical JSON plus explicit HMAC-SHA256 MAC protection, and kept the default stance narrow: proof-backed issuance by default, explicit opt-in for upper-bound issuance, explicit empty-capability tokens for zero-authority, explicit holder and call-chain binding, runtime binding to the chosen M4 runtime, and local-only replay/revocation hooks with no distributed claims. Added checked M6 examples for proof-backed root issuance, upper-bound root issuance, delegated child issuance, upper-bound refusal, and zero-authority, updated `validate_examples.py` to cover success plus replay/audience/holder/passthrough/broadening/runtime/expiry failures, and refreshed `docs/schemas/draft-v1/README.md`, `SPECS.md`, `ARCHITECTURE.md`, and `docs/testing.md` to say plainly that the token path is a draft control-plane MAC workflow rather than public signatures, runtime-general enforcement, or M7 witnessing. Validation passed with `python3 -m py_compile docs/schemas/draft-v1/admission_core.py docs/schemas/draft-v1/minimization_core.py docs/schemas/draft-v1/token_core.py docs/schemas/draft-v1/token_engine.py docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/token_engine.py issue-root ...`, and `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/token_engine.py verify ...`. Remaining explicit limitation is unchanged: draft-v1 still cannot honestly claim runtime-general M5 or M6 enforcement while its schema effect vocabulary remains looser than the live Rust inspect capability surface.
- 2026-03-20T15:46:45Z: Started the draft-bundle M6 token implementation on `docs/schemas/draft-v1/`. Scope is deliberately narrow and fail-closed: add delegation-chain-bound capability-token schema and verification-result schema, implement a draft-local Python issuance/verification path with canonical JSON plus explicit HMAC MAC protection rather than fake signatures, default to proof-backed issuance and refusal when acceptable M5 proof is absent unless upper-bound issuance is explicitly allowed, keep zero-authority explicit through an empty-capability token, add checked examples for root issuance, one-hop delegation, refusal, and zero-authority, and update validator/docs to describe local replay and revocation limits honestly without claiming runtime-general enforcement.
- 2026-03-20T15:15:00Z: Finished the draft-bundle M5 minimization implementation on `docs/schemas/draft-v1/`. Added a new `comparator_profile.schema.json`, widened `proof_record.schema.json` to carry explicit M5 proof-status/search/cache semantics, added `minimization_core.py` plus `minimization_engine.py` for an example-bounded deterministic counterfactual minimizer over admissible M4 plans, tightened runtime-alignment checks to include runtime-guarantee digest identity, and checked in new/adapted example requests, plans, invocation fixtures, comparator profiles, and proof outputs covering exact reduction, no reduction, bounded-minimal shrinking, zero-authority exact minimality, comparator-unavailable fail-closed behavior, and safe cache reuse/bypass. Updated `validate_examples.py`, the draft bundle README, `SPECS.md`, `ARCHITECTURE.md`, and `docs/testing.md` to describe the M5 semantics honestly, including the explicit example-bounded harness limitation. Validation passed with `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/compatibility_check.py`, and `python3 -m py_compile docs/schemas/draft-v1/admission_core.py docs/schemas/draft-v1/admission_engine.py docs/schemas/draft-v1/minimization_core.py docs/schemas/draft-v1/minimization_engine.py docs/schemas/draft-v1/validate_examples.py docs/schemas/draft-v1/compatibility_check.py`.
- 2026-03-20T05:35:00Z: Started the draft-bundle M5 minimization implementation on the live `docs/schemas/draft-v1/` path after confirming the repo no longer has `docs/guild_schemas_v1/`. Scope is intentionally conservative and additive to M4: add an example-bounded deterministic minimization engine plus comparator-profile schema, extend `proof_record` with honest exact-vs-bounded semantics and strict cache identity, reuse the existing Python admission helpers without redesigning M4, add checked proof examples for reduction/no-reduction/zero-authority/comparator failure/cache behavior, and update bundle plus repo docs to say plainly that the M5 harness is not runtime-general.
- 2026-03-20T05:07:27Z: Finished the execution-plan signing follow-on. Added a reusable Rust sign/verify path for `guild.execution_plan` JSON values in `guild-registry` using the existing Ed25519 `LocalPublisherIdentity` and `TrustedPublisherRecord` model, added `guild trust sign-plan` and `guild trust verify-plan` to the real CLI, updated the draft execution-plan signature schema hook from vague `key_id` to `publisher_id` plus explicit format version metadata, and refreshed repo/docs language to say that M4 plans remain unsigned by default but can now be signed and verified later through the existing trust path. Validation passed with `cargo test -p guild-registry --test local_registry execution_plan`, `cargo test -p guild-mcp --test guild_cli trust_`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/compatibility_check.py`, `python3 -m py_compile docs/schemas/draft-v1/admission_core.py docs/schemas/draft-v1/admission_engine.py docs/schemas/draft-v1/validate_examples.py docs/schemas/draft-v1/compatibility_check.py`, and `cargo fmt --all`.
- 2026-03-20T05:00:19Z: Started the execution-plan signing follow-on. Scope is narrow and explicit: reuse the existing publisher identity and trusted-publisher model instead of inventing a second signer surface, add a shared Rust sign/verify path plus minimal CLI commands, keep `admission_engine.py` unsigned by default, and update only the docs/schema hooks needed to describe explicit post-generation plan signing honestly.
- 2026-03-20T04:17:57Z: Finished the M4 closure pass. Added durable repo-memory coverage for the landed draft admission bundle in `MEMORY.md`, added one top-level README pointer so contributors can find the draft bundle and its validation path without mistaking it for canonical product vocabulary, and revalidated the checked Python path with `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/compatibility_check.py`, and `python3 -m py_compile ...` for the draft admission entrypoints. No new engine or schema semantics were added in this closure pass; execution-plan signing remains an explicit follow-on blocker only.
- 2026-03-20T04:17:17Z: Started the M4 closure pass after the admission implementation landed. Scope is deliberately narrow: audit repo-facing docs and durable memory for consistency with the checked-in draft bundle, make it easier to find the bundle validation path without implying canonical status, and keep execution-plan signing explicitly out of scope because the repo still lacks a reusable generic plan-signing path.
- 2026-03-20T04:05:58Z: Finished the draft schema-bundle M4 admission pass on `docs/schemas/draft-v1/`. Added a shared Python admission core plus `admission_engine.py`, introduced `admission_request.schema.json` and `execution_plan.schema.json`, extended `common.schema.json` with stable admission decision/reason-code vocabulary plus zero-authority authority-plan support, replaced the one-off compatibility matcher with a shared hard-requirement truth path, added bundled request/plan examples for `admit`, `downgrade`, `migrate`, and `refuse`, updated `validate_examples.py` for schema validation plus exact deterministic engine-output checks and invalid-runtime negative probes, regenerated `compatibility_matrix.md` as a precheck-only artifact, and updated the draft bundle plus repo docs to say plainly that M4 derives a safe upper-bound execution plan and does not perform M5 minimization. Validation passed with `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py` and `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/compatibility_check.py`. Remaining explicit blocker: execution-plan signing is schema-hook-only because the repo does not yet expose a reusable generic plan-signing path.
- 2026-03-20T03:47:12Z: Started the draft schema-bundle M4 admission implementation on the live `docs/schemas/draft-v1/` path after resolving the stale `docs/guild_schemas_v1/` wording. Scope is intentionally narrow and fail-closed: reuse one shared hard-compatibility path, implement only M4 upper-bound admission rather than M5 minimization, add explicit request/plan schemas plus structured reason codes, make `admit`/`downgrade`/`migrate`/`refuse` concrete with checked examples, and stay honest that execution-plan signing only lands if a real reusable signing primitive already exists.
- 2026-03-20T03:12:22Z: Finished the M3 schema-bundle hardening pass. `docs/schemas/draft-v1/runtime_guarantee.schema.json` now requires explicit `component_model_support.wit_worlds` while still allowing an empty published list, `compatibility_check.py` now fails closed with explicit reasons for omitted or unsupported WIT-world publication and regenerates a matrix that includes the new `examples/zero-authority.contract.json`, `validate_examples.py` now validates that zero-authority example, and the bundle README plus `SPECS.md`, `ARCHITECTURE.md`, ADR 0005, and ADR 0012 now state plainly that the schema bundle remains draft because its effect-class vocabulary still does not fully match the repository’s canonical capability-family surface. Validation passed with an isolated venv at `/tmp/guild-schema-venv`: bundled example schema validation, compatibility-matrix regeneration, positive pass for `zero-authority.contract.json` against `wasmtime-strict.runtime.json`, and explicit negative probes for omitted and unsupported `wit_worlds`.
- 2026-03-20T03:09:04Z: Started the M3 schema-bundle hardening pass for `docs/schemas/draft-v1/`. Scope is narrow and decision-complete: make `component_model_support.wit_worlds` an explicit required runtime guarantee with fail-closed omission handling, add one bundled zero-authority contract example, regenerate the derived compatibility matrix, and update the bundle plus repo docs/ADRs to say exactly where the draft schema vocabulary does and does not match the current canonical runtime capability families.
- 2026-03-20T03:18:00Z: Renamed the draft schema bundle directory from `docs/guild_schemas_v1/` to `docs/schemas/draft-v1/` so the path now carries both draft status and a clearer promotion path. The rename is intentionally not a canonization step: `SPECS.md`, `ARCHITECTURE.md`, and ADRs still describe the bundle as draft vocabulary layered over the current canonical runtime capability-family surface.
- 2026-03-20T02:14:53Z: Finished the AGENTS top-of-file framing pass. Added the exact callout “SANE DEFAULTS. SANE ASSUMPTIONS. SANE EXPECTATIONS. SANE IMPLEMENTATIONS.” directly under the contracts-first opening in `AGENTS.md`, followed by four short lines that tie it to safer defaults, honest assumptions, truthful expectations, and boring explicit host-mediated implementation choices. Verified the new block sits above `## Canonical docs` and that no other opening sections were reordered.
- 2026-03-20T02:14:18Z: Started the AGENTS framing pass to lock a stronger top-of-file operating principle into contributor guidance. This pass is intentionally narrow: add the exact “SANE DEFAULTS. SANE ASSUMPTIONS. SANE EXPECTATIONS. SANE IMPLEMENTATIONS.” callout directly under the contracts-first opening, follow it with a short meaning block that reinforces the repo’s existing boring/explicit/trust-boundary posture, then verify the final placement and capture the result here.
- 2026-03-20T01:41:40Z: Refined GitHub issue `#2` (`[Epic] M0 Lock the invention sentence`) into the fuller invention brief for “Invocation-Specific Authority Admission for Portable Components.” Kept the GitHub epic title and hierarchy unchanged, replaced the placeholder body with the new goal/invention sentence/patent-method framing/term guidance/admission-problem framing, preserved the dependency note alongside `#5` plus the `patent/invention_brief.md` anchor, and stripped only the broken citation-placeholder glyphs while leaving the substantive wording intact.
- 2026-03-20T01:18:46Z: Finished the GitHub patent-tracking pass. Created one umbrella epic plus milestone epics `#2` through `#12` in `jkordish/Guild`, normalized the umbrella and child bodies to use real issue-number links for initial focus and dependencies, and linked all milestone issues as sub-issues under umbrella epic `#1`. Verified the final state through GitHub MCP: 12 open issues total, every issue labeled `enhancement`, and `issue_read get_sub_issues` on `#1` returns the full M0-M10 set.
- 2026-03-20T01:13:59Z: Started the GitHub tracking pass for the patent-oriented milestone set after confirming `jkordish/Guild` now resolves through both GitHub MCP and `gh`, and that the repo currently has no issues or milestones beyond the default labels. This pass is creating one umbrella epic plus milestone epic issues for M0-M10, linking them as sub-issues, and keeping the issue bodies faithful to the user-supplied artifact, done-when, decision-gate, and first-build-slice details.

- 2026-03-19T19:08:08Z: Finished the MCP annotation + pagination hygiene milestone. `guild.inspect` now advertises truthful MCP hints for the real inspect/runtime/storage path, `tools/list`, `resources/list`, and `resources/templates/list` now use bounded opaque cursor pagination with deterministic ordering and fail-closed invalid-cursor handling, the stdio MCP example now proves a paginated templates-list round trip, and the MCP-facing docs/specs were updated to match. Revalidated with `cargo fmt --all`, focused `cargo test -p guild-mcp --test mcp_server_stdio --test guild_cli`, full `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and the example proof sweep `inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `mcp_stdio_local`, `export_import_local`, `export_import_oci_local`, and `push_pull_oci_registry_local`.
- 2026-03-19T17:56:51Z: Started the MCP annotation + pagination hygiene milestone after re-reading `AGENTS.md`, the canonical docs, and the current `guild-mcp` server/tests. Scope is intentionally tight: make `guild.inspect` tool annotations match actual inspect/runtime/storage behavior, add endpoint-scoped opaque cursor pagination for `tools/list`, `resources/list`, and `resources/templates/list` without widening the public surface, update the MCP docs/spec language to match, then rerun fmt/test/clippy plus the touched stdio/example proof flows.
- 2026-03-19T17:46:35Z: Finished the evidence-metadata-resource verification pass. Added one CLI regression that proves `guild read guild://objects/records/{id}` still returns payload bytes while `guild read guild://objects/records/{id}/metadata` returns host-owned `EvidenceRecord` JSON, and strengthened `mcp_stdio_local` so it reads the metadata URI directly and checks the preserved evidence-record URI plus `produced_by_execution` linkage. Revalidated with focused `guild_cli`, `mcp_server_stdio`, and runner resource-read tests, `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and the explicit example proof sweep logged at `target/command-logs/evidence-metadata-proof-sweep-1773942136.log`.
- 2026-03-19T17:16:03Z: Started the evidence-metadata-resource verification pass after re-reading AGENTS plus the canonical docs and confirming the core metadata-URI/backend implementation is already present in the tree. This pass is staying tightly scoped: add explicit CLI and stdio example proof coverage for `guild://objects/records/{evidence_record_id}/metadata`, keep the shared registry/MCP/guest backend unchanged, then rerun focused tests before the full fmt/test/clippy sweep.
- 2026-03-19T06:10:20Z: Finished the `guild codex` workflow pass. Added a shared Codex CLI implementation so `guild codex ...` is now the primary operator surface while `guild-codex` stays as a thin compatibility wrapper, updated the generated helper commands to prefer `guild`, shifted the bootstrap output and docs toward the two recommended deterministic dogfood flows (`recent-failure-triage` and `policy-denial-debug`), and rewired the repo-scoped Codex skill prep scripts onto the new primary path. Revalidated with `cargo fmt --all`, focused `cargo test -p guild-mcp --test codex_workflow --test guild_cli --test mcp_server_stdio`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and direct `guild codex` command checks for `bootstrap`, `print-config`, both recommended `scenario --json` flows, and both recommended `smoke` flows.
- 2026-03-19T03:27:33Z: Started the `guild codex` workflow pass after re-reading AGENTS plus the canonical docs and confirming the current Codex helper path is already real and green under focused regression (`codex_workflow`, `mcp_server_stdio`, and `guild_cli`). This pass is keeping the stdio server and MCP surface unchanged while moving the primary operator story onto `guild codex`, retaining `guild-codex` as a compatibility wrapper, and tightening the printed commands, docs, repo skills, and regression coverage around the two recommended dogfood flows.
- 2026-03-19T02:05:00Z: Finished the `guild list` pass. Added a thin first-class `guild list` command family backed only by existing local-registry truth: `guild list` now summarizes installed skills plus recent persisted executions, while `guild list skills` and `guild list executions --limit <n>` expose each slice directly without implying a live loaded-runtime module registry. Updated the CLI help text, ADR 0019, the command-language doc, the README quickstart, and the testing doc to teach the same honest surface, then revalidated with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, focused `guild_cli` coverage, direct `guild list --help` smoke, and a real temp-root `install -> list -> inspect -> list executions` CLI smoke flow.
- 2026-03-19T01:12:00Z: Started the `guild list` pass after re-reading the current CLI/docs/ADR surface. The honest substrate-backed meaning is now clear: `guild list` should expose installed skills from the local registry plus recent persisted execution activity, with explicit `skills` and `executions` subviews, while avoiding any implication that Guild already tracks a live loaded-runtime module set.
- 2026-03-19T00:06:00Z: Finished the first-class `guild` CLI milestone. Added a real `guild` binary in `crates/guild-mcp`, wired it thinly onto the existing facade/registry/server substrate for `inspect`, `read`, `install`, `export`, `import`, `push`, `pull`, `trust`, and `mcp serve --stdio`, and added the small missing public helpers for canonical `skill://...` parsing, local trust-tier parsing, OCI reference parsing/formatting, and trust-store list/remove operations. Also updated `guild-codex` config generation to launch the new `guild mcp serve --stdio` surface, added CLI integration coverage for inspect/read/install/export/import/push/pull/trust/mcp-stdio plus Codex config regressions, and documented the stable public grammar in `docs/command-language.md` and ADR 0019. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli --test codex_workflow --test mcp_server_stdio`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T20:55:00Z: Started the first-class `guild` CLI milestone after re-reading `AGENTS.md`, `README.md`, `SPECS.md`, `ARCHITECTURE.md`, the ADR index, and the current registry/facade/server seams. This pass is implementing one thin substrate-backed local CLI in `guild-mcp` for inspect/read/install/export/import/push/pull/trust/mcp-stdio, adding only the missing parsing and trust/OCI helper seams needed to keep the CLI honest, then updating the public docs/ADR/workflows and rerunning fmt/test/clippy plus representative CLI smoke flows.
- 2026-03-18T20:27:46Z: Finished the Codex scenario-dogfooding and repo-skills pass. Added `guild-codex scenario` with deterministic `recent-failure-triage`, `policy-denial-debug`, and `execution-tree` flows plus two new smoke flows, expanded bootstrap installs with `inspect-http-json` and `summarize-execution-query`, checked in repo-scoped Codex skills under `.agents/skills`, and rewired `inspect_policy_local`, `explain_recent_failures_local`, and `explain_execution_tree_local` onto the shared scenario helpers. Revalidated with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, the documented `guild-codex` bootstrap/config/scenario/smoke commands, and one successful `scripts/prepare.sh` run for each new repo skill.
- 2026-03-18T19:43:14Z: Verified the new Codex scenario milestone against the real helper path: `cargo test -p guild-mcp --test codex_workflow` now passes with 10 tests covering the expanded bootstrap skill set, the new `guild-codex scenario --json` flows (`recent-failure-triage`, `policy-denial-debug`, `execution-tree`), the two added smoke flows, and the documented `smoke --flow all` sweep over the real stdio MCP server. Next in this pass is repo-scoped Codex skill packaging under `.agents/skills`, then doc/memory updates to make the verified scenario/smoke workflow discoverable.
- 2026-03-18T19:18:26Z: Started the Codex scenario-dogfooding and repo-skills milestone after confirming the existing stdio Codex hookup is already live and green (`cargo test -p guild-mcp --test codex_workflow` and `guild-codex -- bootstrap --json`). This pass is keeping the current stdio/config surface intact, expanding the bootstrapped skill set with query/HTTP helpers, adding shared deterministic `guild-codex scenario` prep flows plus two new smoke flows, then checking in repo-scoped Codex skills under `.agents/skills` for incident triage, policy denial debugging, bundle verification checks, and execution-tree investigation.
- 2026-03-18T18:10:22Z: Started the authority-debug skills milestone after re-reading `AGENTS.md`, `README.md`, `SPECS.md`, `ARCHITECTURE.md`, the ADR index, roadmap, memory files, the current explain/debug example skills, the persisted `ExecutionRecord`/`PolicyDecision` shapes, the trust-tier-aware `inspect_policy_local` flow, and the host HTTP authorization logic. This pass is adding three inspect-only operator-facing example skills (`explain-capability-denial`, `diff-execution-authority`, and `explain-http-authority`) over durable execution resources, extending the existing policy demo to showcase them, updating docs/bootstrap/tests without widening the public MCP surface, and then rerunning fmt/test/clippy plus the touched proof flows.
- 2026-03-18T16:59:57Z: Finished the Codex workflow cleanup pass. `guild-codex` now emits a cwd-independent stdio launcher by using an explicit Cargo manifest path, supports a narrow `smoke` subcommand for the two deterministic Codex dogfood flows, and shares its real stdio smoke implementation with the compatibility examples and the Codex workflow tests. Updated README/AGENTS/MEMORY/example docs to make `bootstrap + print-config + smoke` the preferred helper path while keeping the example commands as compatibility proofs. Reverified with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and the full documented command sweep (helper bootstrap/config/smokes plus every current `guild-mcp` proof example) logged at `target/command-logs/codex-workflow-cleanup-1773852685.log`.
- 2026-03-18T16:02:31Z: Started the Codex workflow cleanup pass after confirming the prior stdio dogfooding milestone is already live in the repo and still green under focused verification (`cargo test -p guild-mcp --test codex_workflow`, `guild-codex -- bootstrap --json`, and `codex_explain_execution_local`). This pass is staying stdio-first/local-first and is focused on one shared `guild-mcp` smoke/helper path, a narrow `guild-codex smoke` command, thinner compatibility examples/tests, and doc output cleanup so the helper, proofs, and docs stop drifting.
- 2026-03-18T15:31:11Z: Re-ran the full verification sweep for the stacked cleanup/review pass before commit. `cargo test --workspace`, the full documented local proof-command regression sweep from `AGENTS.md` (`guild-codex` bootstrap/config plus all current `guild-mcp` examples through `mcp_stdio_local`), and `cargo clippy --workspace --all-targets --all-features -- -D warnings` all passed on the final tree.
- 2026-03-18T15:02:39Z: Finished another `guild-runner` policy review pass. Simplified required-grant marking so `applies_to: required|requested` now treats any grant that overlaps declared required surface as required support, then fixed a separate deny-rule mismatch by making local policy `deny` evaluate typed same-family overlap instead of only matching grants that are fully covered by the deny ceiling. This keeps the policy layer fail-closed and avoids narrower deny rules silently missing broader requests. Added focused runner unit coverage for overlap-based deny matching and refreshed the policy semantics wording in `SPECS.md`, `ARCHITECTURE.md`, and ADRs 0008/0012. Revalidated with `cargo fmt --all`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T11:23:11Z: Finished another `guild-runner` policy review pass. Fixed local policy `cap` handling so multiple same-family cap grants now preserve their union instead of being applied as a destructive sequential intersection, and reclassified required-vs-requested candidate grants after each policy rule so later rules see current host truth rather than stale flags. Added focused unit coverage for split cap reductions and for required-grant marking after a split, then revalidated with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and a final `cargo test -p guild-runner --lib` after the last test-only clippy cleanup.
- 2026-03-18T10:58:12Z: Finished a policy-layer review pass in `guild-runner`. Fixed required-grant classification so local policy `applies_to: required|requested` now tracks grants that actually contribute to satisfying required capabilities, including multi-grant union cases for `invoke-skill`, `read-resource`, and `log-write`, instead of only marking single grants that individually cover a required requirement. Renamed the internal flag from `covers_required` to `contributes_to_required`, added focused unit coverage for union-backed required read-resource and log grants, and revalidated with `cargo fmt --all`, `cargo test -p guild-runner --lib`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T10:33:05Z: Finished another runner review pass focused on the remaining union-vs-single-grant mismatches in capability coverage. Aligned `read-resource` and `log-write` child-grant derivation and required-capability coverage with the runtime's actual per-operation union model, switched read-resource authorization to one per-grant helper path instead of separately stitching kind and scope checks together, and added direct unit coverage for multi-grant read-resource and log requirements. Revalidated with `cargo fmt --all`, `cargo test -p guild-runner --lib`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T10:02:47Z: Finished a follow-up runner consistency pass. Aligned `CapabilityEvaluator::grants_cover_requirement` with the new invoke-dependency union model so required multi-alias invoke surfaces can be satisfied by the union of multiple exact parent grants, not just by one all-covering grant. Revalidated with `cargo fmt --all`, `cargo test -p guild-runner --lib`, and `cargo test --workspace && cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T09:39:06Z: Finished another runner review pass focused on composite child-grant derivation semantics. Kept exact alias matching strict, but changed invoke-dependency grant reduction to compose exact alias subsets across multiple parent grants instead of requiring one parent grant to cover every requested alias. Added direct unit coverage for exact alias reduction, exact coverage, union success across multiple parent invoke grants, and fail-closed behavior when the union is incomplete. Revalidated the current tree with `cargo fmt --all`, `cargo test -p guild-runner --lib`, and `cargo test --workspace && cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T09:01:25Z: Finished a runner-focused review pass. Found and corrected a semantics mismatch in `crates/guild-runner/src/lib.rs`: `reduce_required_exact_string_scope` was using prefix matching even though alias-scoped dependency grants are contractually exact-match only. Simplified both exact-string helpers to share the same exact-set logic and added direct unit coverage proving prefixes do not satisfy exact alias scopes. Revalidated the full current tree with `cargo fmt --all` and `cargo test --workspace && cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T08:37:57Z: Finished a deeper second review pass focused on production-path simplicity in `guild-registry` and OCI transport after the earlier test/support cleanup. Removed redundant owned clones in bundle import, evidence record persistence, and OCI validation error details; collapsed duplicated post-import verification/loading work into one path; and simplified the spawned OCI registry worker join handling without changing fail-closed behavior. Revalidated the full current tree with `cargo fmt --all` and `cargo test --workspace && cargo clippy --workspace --all-targets --all-features -- -D warnings`.
- 2026-03-18T08:12:45Z: Finished the code-review and cleanup pass. Consolidated duplicated `guild.inspect` MCP JSON/request parsing helpers into `test-support/guild_inspect_helpers.rs`, simplified repeated execution-record/explain assertions in `crates/guild-mcp/tests/inspect_facade.rs`, cleaned up a few naming and string-construction hazards surfaced by lint, and made the deterministic HTTP test server append the redirect header without allocating an intermediate formatted string. Revalidated the final tree with `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo clippy -p guild-mcp --test inspect_facade -- -W clippy::pedantic`.
- 2026-03-18T06:30:43Z: Started a code-review and cleanup pass after re-reading `AGENTS.md`, `README.md`, `SPECS.md`, and `ARCHITECTURE.md`. The first repair target is maintainability rather than contract drift: consolidate duplicated `guild.inspect` MCP request helpers used by Codex-path tests/examples, clean up small naming hazards surfaced by pedantic lint, and simplify the shared deterministic HTTP test server so the support code stays boring and predictable.
- 2026-03-18T05:12:25Z: Finished the evidence-metadata-resource-URI milestone. Added a first-class companion metadata URI shape for evidence records, kept `guild://objects/records/{id}` as the payload-dereference path, wired metadata reads through the shared registry backend used by guest `read-resource` and MCP `resources/read`, updated `explain-execution-tree` to consume evidence metadata resources instead of payload reads for its optional descriptors, and refreshed the canonical specs/architecture/ADR/repo-memory docs to describe the payload-vs-metadata split honestly. Reverified with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo run -q -p guild-mcp --bin guild-codex -- bootstrap --registry-root target/dev-local-registry/codex-local --reset`, `cargo run -q -p guild-mcp --bin guild-codex -- print-config --registry-root target/dev-local-registry/codex-local`, and the full documented `guild-mcp` proof sweep (`inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `filesystem_rejection_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `explain_recent_failures_local`, `codex_explain_execution_local`, `codex_explain_execution_tree_local`, `export_import_local`, `export_import_oci_local`, `export_import_composite_local`, `export_import_composite_oci_local`, `signed_import_failures_local`, `signed_import_oci_failures_local`, `push_pull_oci_registry_local`, `push_pull_composite_oci_registry_local`, `signed_pull_oci_registry_failures_local`, and `mcp_stdio_local`).
- 2026-03-18T04:03:25Z: Started the evidence-metadata-resource-URI milestone. Re-read `AGENTS.md`, `README.md`, `SPECS.md`, `ARCHITECTURE.md`, the ADR index, roadmap, memory files, and the current evidence/resource code path; confirmed the live gap is that `guild://objects/records/{id}` still dereferences to payload bytes while host-side `load_evidence_record` already exposes durable metadata. The implementation plan is to add one additive metadata URI under the existing object-record scope, route guest and MCP reads through the same registry backend, patch one explain/debug path to consume metadata resources, update the contract/ADR/docs baseline, and reverify with fmt/tests/clippy plus the documented proof flows.
- 2026-03-18T03:55:00Z: Finished the current-tree verification sweep for the unsupported-runtime-surface milestone. `cargo test --workspace`, the full documented `guild-mcp` regression proof batch, and `cargo clippy --workspace --all-targets --all-features -- -D warnings` all passed again on the exact tree being committed.
- 2026-03-18T03:33:29Z: Started final verification-and-commit pass for the unsupported-runtime-surface milestone on the current tree: rerunning `cargo test --workspace`, the documented `guild-mcp` regression proof examples, and `cargo clippy --workspace --all-targets --all-features -- -D warnings` before creating one commit that keeps the runner, tests, and contract/doc updates together.
- 2026-03-18T03:15:43Z: Finished the unsupported-runtime-surface normalization milestone. Centralized host-owned unsupported-runtime-surface classification in `guild-runner`, removed duplicated unsupported-family handling from inspect projection/runtime validation, added Wasmtime component import-surface preflight so the active inspect runtime only accepts `guild:skill/inspect-types@1.0.0` and `guild:skill/inspect-host@1.0.0`, and added regression coverage proving broader Guild imports are rejected before guest execution while policy denial remains distinct in durable records and explain/debug flows. Updated the canonical specs/architecture/ADR/AGENTS/MEMORY docs to make the unsupported-runtime-surface story explicit, then reverified with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and the full documented `guild-mcp` proof sweep (`inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `filesystem_rejection_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `explain_recent_failures_local`, `codex_explain_execution_local`, `codex_explain_execution_tree_local`, `export_import_local`, `export_import_oci_local`, `export_import_composite_local`, `export_import_composite_oci_local`, `signed_import_failures_local`, `signed_import_oci_failures_local`, `push_pull_oci_registry_local`, `push_pull_composite_oci_registry_local`, `signed_pull_oci_registry_failures_local`, and `mcp_stdio_local`).
- 2026-03-18T02:22:57Z: Started the unsupported-runtime-surface normalization milestone. Re-read AGENTS plus the canonical docs, traced runner preflight/projection/termination behavior, confirmed unsupported capability-family rejection already exists but is duplicated, and locked the implementation around one centralized host-owned unsupported-runtime-surface classifier, Wasmtime component import-surface preflight for broader Guild imports, a deliberately broad-world test fixture under an inspect manifest, explain/debug distinction coverage, coordinated doc updates, and a full fmt/test/clippy plus proof-flow verification sweep.
- 2026-03-18T01:23:22Z: Finished the explicit inspect projection-contract milestone. Added a centralized `guild-runner` inspect projection module that owns host-to-guest projection for the active inspect slice, made the execution-context subset rule and per-family grant projection contracts explicit and test-backed, added regression coverage for active-family projection, unsupported-family non-projection, full HTTP projection shape, and durable host-truth vs guest-truth behavior, updated the canonical specs/architecture/ADR/AGENTS/MEMORY guidance to describe projection rather than accidental isomorphism, and reverified with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, plus the full documented `guild-mcp` proof sweep (`inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `filesystem_rejection_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `explain_recent_failures_local`, `codex_explain_execution_local`, `codex_explain_execution_tree_local`, `export_import_local`, `export_import_oci_local`, `export_import_composite_local`, `export_import_composite_oci_local`, `signed_import_failures_local`, `signed_import_oci_failures_local`, `push_pull_oci_registry_local`, `push_pull_composite_oci_registry_local`, `signed_pull_oci_registry_failures_local`, and `mcp_stdio_local`).
- 2026-03-18T00:26:00Z: Started the explicit inspect projection-contract milestone. Re-read AGENTS plus the canonical docs and ADRs, traced the active `guild-skill-inspect-v1` WIT, runner execution path, durable record shapes, and current projection helpers, and confirmed the remaining work is to centralize the host-to-guest projection boundary into one runner module, make per-family projection contracts explicit for the five active inspect families, add projection-specific regression coverage for host-truth vs guest-truth and unsupported-family non-projection, then update the spec/architecture/ADR guidance before rerunning fmt/test/clippy and the documented proof flows.
- 2026-03-17T19:52:28Z: Finished the Codex stdio dogfooding milestone. Added the narrow `guild-codex` helper plus shared `guild_mcp::codex` bootstrap/config support so a fresh local registry root can be bootstrapped, the required inspect-only example skills installed, and the exact `codex mcp add ... -- cargo run -q -p guild-mcp --bin guild-mcp-server --` command or matching `mcp_servers.*` TOML emitted without widening the public MCP surface. Added deterministic MCP-path `codex_explain_execution_local` and `codex_explain_execution_tree_local` proof examples plus a shared stdio client harness and `codex_workflow` integration tests that verify helper/config correctness, real stdio server startup, and resource-producing dogfood flows. Updated README, AGENTS, durable memory, and example READMEs with the real Codex workflow and explicitly deferred scope. Reverified with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo run -p guild-mcp --bin guild-codex -- bootstrap --registry-root target/dev-local-registry/codex-local --reset`, `cargo run -p guild-mcp --bin guild-codex -- print-config --registry-root target/dev-local-registry/codex-local`, and the full documented `guild-mcp` proof sweep including the new Codex-path examples.
- 2026-03-17T19:06:39Z: Started the Codex stdio dogfooding milestone. Re-read `AGENTS.md`, `README.md`, `SPECS.md`, `ARCHITECTURE.md`, the ADR index, roadmap, and durable memory; inspected the current `guild-mcp-server` entrypoint, stdio protocol tests, and proof examples; confirmed the local Codex CLI plus official OpenAI Codex docs support the exact stdio MCP setup we need (`codex mcp add ... -- <command>` and `mcp_servers.*` in `config.toml`); and locked the implementation path around one thin local workflow helper, deterministic MCP-path dogfood flows, doc updates, and smoke coverage without widening Guild's public MCP surface.
- 2026-03-17T18:10:45Z: Finished the filesystem policy-contract guardrail milestone. Added an explicit host-side typed `filesystem` capability contract (`CapabilityId::Filesystem`, `CapabilityConstraints::Filesystem`, `FilesystemConstraints`, `FilesystemRoot`, `FilesystemOperation`), wired intentional preflight rejection for filesystem activation in the active Wasm inspect slice with host-owned `filesystem-runtime-not-supported` denials before guest start, preserved the guest ABI as filesystem-free, added regression coverage for manifest/grant/policy/explain behavior plus the `filesystem_rejection_local` proof example, updated the canonical docs/ADR text to mark filesystem as design-only/deferred runtime, and reverified with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and the full documented `guild-mcp` example proof sweep (`inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `filesystem_rejection_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `explain_recent_failures_local`, `export_import_local`, `export_import_oci_local`, `export_import_composite_local`, `export_import_composite_oci_local`, `signed_import_failures_local`, `signed_import_oci_failures_local`, `push_pull_oci_registry_local`, `push_pull_composite_oci_registry_local`, `signed_pull_oci_registry_failures_local`, `mcp_stdio_local`).
- 2026-03-17T16:55:40Z: Started the filesystem contract guardrail milestone. Confirmed the current shared Rust capability surface still omitted filesystem entirely while the runner only had a generic unsupported-family preflight, so this pass is adding an explicit host-side typed filesystem contract, dedicated preflight rejection in the active inspect slice, regression coverage for persisted rejections and explain flows, and one deterministic local proof example without widening the guest ABI or adding runtime file IO.
- 2026-03-17T16:03:35Z: Finished the verification-first HTTP hardening pass with no implementation gaps found. `cargo fmt --all --check`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and the full documented `guild-mcp` example proof sweep all passed (`inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `explain_recent_failures_local`, `export_import_local`, `export_import_oci_local`, `export_import_composite_local`, `export_import_composite_oci_local`, `signed_import_failures_local`, `signed_import_oci_failures_local`, `push_pull_oci_registry_local`, `push_pull_composite_oci_registry_local`, `signed_pull_oci_registry_failures_local`, `mcp_stdio_local`). Final verdict: the HTTP hardening milestone is already implemented end to end, so no code/docs changes were made beyond this timestamped verification log.
- 2026-03-17T16:03:35Z: Started a verification-first pass for the HTTP hardening milestone after the explicit no-cosmetic-change instruction. Reconfirmed the repo already contains typed `http-request` constraints, host-owned redirect and risky-destination enforcement, trust-tier/profile shaping, and nested child grant reduction; now running the full fmt/test/clippy and documented proof sweep to either confirm the milestone is already complete or surface only real remaining gaps.
- 2026-03-17T04:58:50Z: Finished the HTTP policy hardening milestone. Hardened `http-request` with exact hosts plus domain suffixes, explicit redirect controls, loopback/link-local/private-network/IP-literal enforcement, trust-tier/profile-driven HTTP reduction, expanded denial coverage, updated docs/ADR 0017, and reverified with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, plus the full documented `guild-mcp` example proof sweep.
- 2026-03-17T03:55:33Z: Started the HTTP policy hardening milestone. Re-read AGENTS plus the canonical docs and ADR 0017, confirmed the current `http-request` family still only enforces exact host/method/scheme/port/path plus timeout/response-size bounds, and began the implementation pass to add host-side typed redirect and risky-destination controls without widening the guest ABI.
- 2026-03-17T03:30:00Z: Finished the per-family policy ADR freeze pass. Added ADRs 0012 through 0018 for policy layering, read-resource, invoke-skill, emit-evidence, log-write, http-request, and design-only filesystem policy semantics; updated the ADR index plus small policy ADR cross-references; and reverified the tree with `git diff --check` and `cargo test --workspace`.
- 2026-03-17T03:15:32Z: Started the per-family policy ADR freeze pass. Reviewed the canonical docs, existing ADRs, WIT, policy/profile types, runner authorization and child-grant reduction logic, registry policy loading, and the inspect/composite/http/resource proof coverage to capture current policy truth without changing runtime semantics.
- 2026-03-17T05:05:00Z: Fixed a stale-guest-artifact regression by making every example skill crate track `wit/guild-skill-v1.wit` via `include_str!`, revalidated `cargo test --workspace`, and reran the full documented `guild-mcp` example proof sweep successfully. Also staged permanent cleanup of the tracked nested `examples/*/skill-rust/target` trees with `git rm --cached` so future Cargo builds stop dirtying `git status`, and normalized the workspace plus example-skill `Cargo.toml` files through `cargo-tomlfmt` output promotion.
- 2026-03-18T19:02:24Z: Finished the authority-debug inspect skill milestone. Added the inspect-only `explain-capability-denial`, `diff-execution-authority`, and `explain-http-authority` example skills plus shared parsing/evaluation helpers over persisted execution resources, extended `inspect_policy_local` into the main operator proof flow, updated Codex bootstrap + MCP integration coverage to install/exercise the new skills without widening the public MCP surface, and revalidated with `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and fresh proof runs for `inspect_policy_local` and `inspect_http_json_local`.
- 2026-03-17T04:05:00Z: Audited the bounded local artifact-query milestone against AGENTS plus the canonical docs. The current tree already contains the execution-query resource layer, MCP resource templates, query-scope authorization, the `summarize-execution-query` inspect-only example skill, the `explain_recent_failures_local` proof flow, and targeted regression coverage; the remaining work in this pass is re-verifying the documented proof commands plus `cargo fmt --all` and `cargo test --workspace` on the clean checkout.
- 2026-03-17T03:46:00Z: Final verification is clean for the bounded query-resource milestone. `cargo fmt --all`, `cargo run -p guild-mcp --example explain_recent_failures_local`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic -W clippy::cargo -W clippy::future_not_send` all pass on the final tree.
- 2026-03-17T02:41:00Z: Fixed the new query-summary skill and finished the focused integration sweep. The guest skill now mirrors the generated WIT ABI correctly, exports its world explicitly, and the query-scope auth test now asserts the actual host-owned preflight rejection path instead of a guest-time read error. `cargo test -p guild-registry --test local_registry`, `cargo test -p guild-runner --test resource_reads`, `cargo test -p guild-manifest --test validation`, `cargo test -p guild-mcp --test mcp_server_stdio`, and `cargo test -p guild-mcp --test inspect_facade` are all green again.
- 2026-03-17T02:09:00Z: Landed the bounded execution-query milestone core. Added `ResourceKind::Query`, `GuildResourceScope::ExecutionQuery`, typed bounded execution-query resource/result types, one shared registry-backed query evaluator over persisted execution records, new MCP resource templates for recent/failures/by-status/by-skill query URIs, the inspect-only `summarize-execution-query` example skill, and the `explain_recent_failures_local` proof flow.
- 2026-03-17T00:32:53Z: Started the bounded local artifact-query milestone. Confirmed the current resource contract only models execution and object URIs, MCP `resources/list` is a bounded recent-execution projection over the registry, and guest plus MCP reads already converge on `LocalRegistry::read_resource`; the implementation path is to add one new query resource kind/scope rooted at `guild://queries/executions/`, keep queries path-shaped and bounded, project them through the existing registry resource backend, and prove the model with one inspect-only query-summary skill plus a deterministic local recent-failures flow.
- 2026-03-17T00:06:45Z: Finished verification for the OCI registry transport milestone. `cargo fmt --all`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic -W clippy::cargo -W clippy::future_not_send` now all pass cleanly after one final derive cleanup in `guild-registry` and small example-output fixes in the new OCI registry proof binaries.
- 2026-03-16T23:43:01Z: Landed the OCI registry transport milestone core plus proof flows. `guild-registry` now pushes and pulls the existing OCI-mapped signed installed bundle through a real OCI registry reference, reconstructs the signed bundle locally on pull, and reuses the existing trust/signature/install path; new registry tests, MCP execution tests, and proof examples cover primitive/composite happy paths plus untrusted and tampered failures through a deterministic local registry harness.
- 2026-03-16T23:01:55Z: Started the OCI registry transport milestone. Confirmed the honest reuse point is the existing ADR 0009 OCI artifact mapping in `guild-registry`, and the implementation path is to push/pull that same signed installed-bundle transport through a deterministic local OCI registry while keeping local publisher trust and signature verification as the final install gate.
- 2026-03-16T21:17:53Z: Re-ran the strict workspace lint pass after the final trust-tier policy cleanup. `cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic -W clippy::cargo -W clippy::future_not_send` now passes cleanly after splitting the new policy evaluator and policy proof example into smaller helpers and tightening the registry import helpers.
- 2026-03-16T21:12:14Z: Finished verification for the richer trust-tier policy milestone. `cargo fmt --all`, `cargo test -p guild-mcp --tests`, and `cargo test --workspace` all passed after tightening `inspect_policy_local` to prove not just trusted-vs-restricted imported HTTP outcomes but also explanation of the persisted restricted denial through `explain-execution`. Fresh live proof runs succeeded for `inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, and `export_import_local`.
- 2026-03-16T20:40:31Z: Landed the core richer-policy implementation. `guild-types` now has host-owned local trust tiers, verification-state enums, named policy profiles/bindings, and richer durable `PolicyDecision` metadata; `guild-registry` now derives install trust from source-vs-verified state plus trusted publisher records; `guild-runner` now selects named profiles, applies trust-aware capability rules, and persists profile/tier context in denials and reductions; `inspect_policy_local` plus MCP integration tests now prove trusted-vs-restricted imported HTTP behavior under local policy profiles.
- 2026-03-16T20:04:23Z: Started the richer local policy milestone. Confirmed the current repository still has one flat local `policy.json` plus a narrow `verified_publishers_required_for` hook, while host-owned install trust is only implicit in `verification.json` and trusted publisher records. The implementation path is to add explicit host-owned local trust tiers, named policy profiles with actor/tenant bindings, richer durable `PolicyDecision` metadata, and one deterministic trust-tier-aware HTTP proof flow without changing the public MCP surface.
- 2026-03-16T19:45:44Z: Re-ran the strict workspace lint command for the OCI portability milestone after the final `oci_layout.rs` cleanup. `cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic -W clippy::cargo -W clippy::future_not_send` now passes cleanly, so the branch is ready for staging and commit.
- 2026-03-16T19:41:17Z: Finished verification for the OCI portability milestone. `cargo fmt --all` and `cargo test --workspace` both passed, and the documented native + OCI portability proof binaries all succeeded: `export_import_local`, `export_import_oci_local`, `export_import_composite_local`, `export_import_composite_oci_local`, `signed_import_failures_local`, and `signed_import_oci_failures_local`.
- 2026-03-16T19:28:05Z: Landed the core OCI portability implementation: `guild-registry` now assembles one shared signed installed-bundle payload for both native bundle and OCI export, OCI layout import validates descriptor structure/digests before reconstructing the signed bundle and delegating to the existing trust/signature/install path, registry + facade regression tests now cover primitive/composite/untrusted/tampered OCI flows, and new `guild-mcp` proof binaries plus doc/ADR updates describe OCI image layout as an additional local transport rather than a replacement for the native signed bundle.
- 2026-03-16T19:12:26Z: Started the OCI portability milestone. Confirmed the current portability boundary lives in `guild-registry` as installed-state bundle assembly plus fail-closed trust/signature/import staging, and the implementation plan is to keep those signed bundle semantics canonical while adding a local OCI image layout transport that reconstructs the same installed executable state.
- 2026-03-16T02:56:23Z: Created `WORKING_MEMORY.md` and started updating repository instructions so `MEMORY.md` remains the durable final tracking output while short-term task notes live here.
- 2026-03-16T02:56:57Z: Updated `AGENTS.md` to formalize the tracking split: `MEMORY.md` for durable repo state and `WORKING_MEMORY.md` for timestamped short-term progress notes during active work.
- 2026-03-16T03:02:19Z: Renamed the legacy tracking filenames to `MEMORY.md` and `WORKING_MEMORY.md`, then updated repo references to use the paired uppercase convention.
- 2026-03-16T03:17:51Z: Began implementing `explain-execution-tree` as a bounded inspect-only example skill over persisted execution lineage and evidence records, starting from the existing `explain-execution` and composite proof paths.
- 2026-03-16T03:46:49Z: Verified the new tree skill end to end with `cargo fmt --all`, `cargo test --workspace`, and the documented local proof commands for inspect, composite inspect, explain, explain-failure, explain-execution-tree, and signed local export/import composite flows.
- 2026-03-16T03:48:07Z: Updated `AGENTS.md` to make research order explicit: use local repo truth first, then loaded MCP servers, with Context7 as the default first stop for library/framework docs and web search only as fallback or clarification.
- 2026-03-16T03:55:00Z: Cleaned up accidentally tracked nested example build artifacts, tightened `.gitignore` to ignore `**/target/`, and prepared a follow-up commit so the branch keeps source changes but not generated outputs.
- 2026-03-16T04:21:40Z: Started the HTTP capability milestone implementation after a repo-grounded plan: keep the Guild guest ABI stable, add typed `http-request` constraints plus request/response types, back the Wasm host import with `wasmtime-wasi-http`, add one primitive `inspect-http-json` proof skill, and cover nested grant reduction with a narrow integration test instead of a second showcase flow.
- 2026-03-16T05:15:22Z: Landed the core runner-side HTTP boundary: typed WIT `http-request`/`http-response` messages, host-side URL parsing plus scheme/host/port/path/method/timeout enforcement, child-grant reduction and requirement coverage for `http-request`, a real Wasmtime-backed outbound request path using `wasmtime-wasi-http`, response byte and between-bytes timeout enforcement, persisted `network_requests` metrics, and honest inclusion of `http-request` in the active Wasm inspect capability allowlist only now that the host path compiles.
- 2026-03-16T05:39:54Z: Finished the HTTP capability milestone end to end: added the primitive `inspect-http-json` source skill, a reusable deterministic local HTTP test server, runner and MCP integration coverage for happy-path/denial/bounds/explain/nested-grant behavior, updated contract/architecture/ADR/docs to list `http-request` as active only now that the runtime path is real, and re-verified with `cargo test --workspace`, `cargo fmt --all`, and `cargo run -p guild-mcp --example inspect_http_json_local`.
- 2026-03-16T17:12:47Z: Started the local policy milestone after tracing the current authority path: `guild-mcp` still stamps caller-requested grants directly into `ResolvedExecutionEnvelope.granted_capabilities`, child execution uses the same shortcut, and installed verification metadata is available in the registry but not yet used for runtime policy. The implementation will add a typed local `policy.json`, a host-owned evaluator that clamps requested grants to the manifest-declared capability surface before applying local rules, and proof/test coverage for denied and reduced inspect executions.
- 2026-03-16T17:46:31Z: Wired the local policy evaluator through `guild-mcp -> runner -> registry`, added typed local policy config and policy decision reasons, updated explain/debug coverage to surface persisted `policy_decision`, and aligned the canonical docs plus a new ADR around the new requested-vs-granted separation. Remaining work is the final verification sweep: format, full workspace tests, and the local proof commands including the new `inspect_policy_local` flow.
- 2026-03-16T18:13:03Z: Finished the verification sweep. `cargo fmt --all` and `cargo test --workspace` now pass after updating one stdio MCP expectation from `capability-mismatch` to `policy-denied` and aligning a legacy runner test fixture summary with the new default policy wording. Fresh proof runs succeeded for `inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `export_import_local`, `export_import_composite_local`, `signed_import_failures_local`, and `mcp_stdio_local`.
- 2026-03-16T18:15:41Z: Starting a strict lint cleanup pass before commit. Plan is to run the repo’s full Clippy command (`--workspace --all-targets --all-features` plus pedantic/cargo/future_not_send warnings), patch the findings surgically, rerun lint cleanly, and then commit the complete local-policy milestone plus any necessary generated artifacts together.
- 2026-03-16T18:31:56Z: Strict lint cleanup is complete. `cargo clippy --workspace --all-targets --all-features -- -W clippy::pedantic -W clippy::cargo -W clippy::future_not_send` now passes cleanly after tightening the HTTP authorization helpers, converting the HTTP test-server host helper to an associated function, and annotating a few intentionally large test-only fixture functions. Re-ran `cargo test --workspace` after the lint fixes and the workspace is green again.
2026-03-17T03:01:47Z dependency-upgrade verification: workspace tests passed after schemars 1.2 + wit-bindgen 0.54 update; next running fmt, clippy, and full proof sweep before commit.
2026-03-17T03:07:48Z verification complete: cargo fmt --all, cargo test --workspace, cargo clippy --workspace --all-targets --all-features, and full documented guild-mcp proof sweep all passed after dependency updates.
- 2026-03-17T20:43:36Z: Started the Rust 2024 / MSRV 1.94 update. Pinned `rust-toolchain.toml` to `1.94.0`, moved the workspace package edition to `2024`, added workspace-inherited `rust-version = 1.94` for all workspace crates, updated every standalone example skill crate to edition 2024 with the same MSRV, and aligned the embedded runner test fixture manifests so the edition/toolchain contract stays honest before rerunning fmt, clippy, and workspace tests.
- 2026-03-17T21:09:43Z: Finished the Rust 2024 / MSRV 1.94 update. Added `wasm32-wasip2` to `rust-toolchain.toml` so fresh toolchain installs can still build the example skills, fixed newly surfaced Rust 1.94 clippy `collapsible_if` findings in `guild-types`, `guild-runner`, and `guild-mcp`, and revalidated the upgraded workspace with `cargo fmt --all`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and `cargo test --workspace` on Rust 1.94.0.
- 2026-03-17T22:43:20Z: Re-ran the full post-upgrade verification before commit. `cargo clippy --workspace --all-targets --all-features -- -D warnings` passed again, `cargo test --workspace` passed again, and the full documented local regression sweep completed successfully with logs under `target/command-logs/rust-2024-regressions-1773786948`.
- 2026-03-17T23:01:00Z: Started the ABI-honesty milestone for the active inspect slice. Re-read `README.md`, `SPECS.md`, `ARCHITECTURE.md`, ADRs 0003/0005/0012/0017/0018, and the current WIT/runner/tests path; confirmed the live drift is that Wasmtime/example skills still bind the broad `guild-skill` world while the runtime enforces a narrower inspect-only surface by convention and preflight checks. Plan is to add a dedicated inspect world, switch manifests/examples/runner to it, make the host-to-guest capability projection explicit, add parity/unreachability regression coverage, then rerun the full verification and documented proof flows.
- 2026-03-17T23:54:00Z: Landed the core inspect-world split. `wit/guild-skill-v1.wit` now defines the dedicated `guild-skill-inspect-v1` world with only the active inspect imports and capability IDs; inspect manifests/example skills/test-built fixture guests now target that world; the Wasmtime runner instantiates only that world and uses an explicit host-to-guest inspect projection for execution context and grants; runner regression coverage now checks inspect-world parity, non-inspect ABI rejection, and guest-visible HTTP grant projection; docs/ADRs/specs/MEMORY/AGENTS now describe the active inspect ABI truth. Remaining work in this pass is the full workspace `test`/`clippy` and documented proof-flow verification.
- 2026-03-17T23:59:20Z: Finished ABI-honesty verification. `cargo fmt --all`, `cargo test --workspace`, and `cargo clippy --workspace --all-targets --all-features -- -D warnings` all passed, and the documented local proof sweep succeeded for `inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `filesystem_rejection_local`, `inspect_composite_local`, `explain_execution_local`, `explain_execution_tree_local`, `explain_failure_local`, `explain_recent_failures_local`, `codex_explain_execution_local`, `codex_explain_execution_tree_local`, `export_import_local`, `export_import_oci_local`, `export_import_composite_local`, `export_import_composite_oci_local`, `signed_import_failures_local`, `signed_import_oci_failures_local`, `push_pull_oci_registry_local`, `push_pull_composite_oci_registry_local`, `signed_pull_oci_registry_failures_local`, and `mcp_stdio_local`. Provenance and guest-visible outputs consistently report `abi: "guild-skill-inspect-v1"` after the split.
- 2026-03-18T05:48:09Z: Re-ran final verification for the evidence-metadata-resource milestone before commit. `cargo test --workspace` passed again, the full documented local regression sweep (including Codex bootstrap/stdio, inspect, explain, portability, OCI layout, and OCI registry flows) completed successfully again, and `cargo clippy --workspace --all-targets --all-features -- -D warnings` passed cleanly on the final tree.
- 2026-03-18T23:19:21Z: Tightened the top-level docs after landing the real `guild` CLI. `README.md` is now focused on what Guild is plus the main local `guild` usage flows, the long proof/test command inventory moved to `docs/testing.md`, and contributor-facing docs now point to `docs/testing.md` instead of treating the README as the exhaustive proof-path reference.
- 2026-03-18T23:58:27Z: Final verification for the thin first-class `guild` CLI plus docs pass is green. `cargo fmt --all`, `cargo test --workspace`, the documented regression sweep in `docs/testing.md` (real CLI smokes, Codex helper flows, and all current `guild-mcp` proof examples), and `cargo clippy --workspace --all-targets --all-features -- -D warnings` all passed; the end-to-end regression command log is at `target/command-logs/regressions-1773877570.log`.
- 2026-03-19T00:36:49Z: Tightened ADR 0019 and the thin `guild` CLI contract language without changing runtime behavior. The ADR now explicitly names canonical URI families, canonical-vs-convenience skill syntax, the no-implicit-root rule, and the local-only scope of `guild trust ...`; the public command-language doc, README, CLI help/error text, and focused CLI regression tests were updated to match. Revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli --test codex_workflow --test mcp_server_stdio`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, plus direct `guild --help` and `guild inspect --help` smoke checks.
- 2026-03-19T06:32:26Z: Started the explicit pre-commit verification sweep for the `guild codex` workflow pass at user request. Current tree matches the intended CLI/docs/test changes; next steps are a fresh `cargo test --workspace`, the documented local regression flows for Codex/bootstrap/explain/portability/MCP, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and then a non-interactive commit if everything stays green.
- 2026-03-19T08:35:22Z: Finished the explicit pre-commit verification sweep for the `guild codex` workflow pass. `cargo test --workspace` passed cleanly after stopping one stale background duplicate test process from an earlier interrupted attempt, the full documented regression sweep in `docs/testing.md` passed with log `target/command-logs/regressions-1773907393.log`, `cargo fmt --all` passed, and `cargo clippy --workspace --all-targets --all-features -- -D warnings` passed on the final tree; next step is the non-interactive commit.
- 2026-03-19T14:38:17Z: Started a review-first, bug-fix-second correctness pass across the current Guild surface after re-reading `AGENTS.md`, the canonical docs, and the core registry/runner/MCP seams. This pass is findings-first: confirm real bugs or contract drift in identity/install, execution persistence, capability/policy enforcement, inspect ABI projection, HTTP bounds, MCP/CLI honesty, and doc alignment; patch only substantiated issues with focused regression coverage; then rerun fmt, workspace tests, clippy, and the touched proof flows.
- 2026-03-19T15:35:25Z: Finished the correctness pass. Confirmed one real code bug in recent/query execution ordering: the registry compared RFC3339 timestamps as raw strings, which can misorder same-second records when one timestamp includes fractional seconds and another does not. Fixed the comparator to parse RFC3339 timestamps before falling back to raw strings, added a regression test for mixed fractional/non-fractional timestamps, and corrected a small contract-drift note in `docs/spec-delta-guest-abi-host-record-boundary.md` so it matches the active inspect ABI and current execution-envelope shape. Revalidated with focused runner/registry/MCP suites, `cargo fmt --all`, `cargo test --workspace` (after one transient `ring` dep-info artifact failure on the first attempt, then passing cleanly on rerun), `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and the touched proof flows `cargo run -p guild-mcp --example explain_recent_failures_local` and `cargo run -p guild-mcp --example mcp_stdio_local`.
- 2026-03-19T15:38:37Z: Started a second review-first correctness pass on the post-fix tree at user request. This pass is re-checking the remaining highest-risk seams without widening scope: trust/import/export metadata, host-owned execution/evidence persistence, policy/runtime/MCP truth alignment, active inspect ABI docs vs code, HTTP enforcement, and CLI/stdio honesty. Default outcome is no edits unless a new issue is substantiated by code inspection plus focused tests or proof flows.
- 2026-03-19T16:09:05Z: Completed the second review-first correctness pass without substantiating a new repo bug worth patching. Re-checked the registry import/export/trust metadata paths, runner/MCP receipt/resource/error behavior, and HTTP/policy enforcement against the current contracts, then ran the required validation suite plus representative proof flows: `inspect_local`, `inspect_http_json_local`, `inspect_policy_local`, `inspect_composite_local`, `explain_execution_tree_local`, `explain_failure_local`, `export_import_local`, `push_pull_oci_registry_local`, `signed_import_failures_local`, and `mcp_stdio_local`. Everything passed on the current committed tree, so the pass intentionally stopped without further code churn.
- 2026-03-19T19:13:52Z: Started the sane-local-defaults and explicit Codex-setup milestone after re-reading `AGENTS.md`, `README.md`, `docs/command-language.md`, `docs/testing.md`, `SPECS.md`, `ARCHITECTURE.md`, `docs/adr/README.md`, `docs/roadmap.md`, and ADR 0019, then inspecting the current `guild` CLI and `guild codex` seams. This pass is intentionally tight: move operator root precedence to `--registry-root` > `GUILD_REGISTRY_ROOT` > `~/.guild`, keep read-only paths from silently creating a fresh root, add an explicit `guild codex setup` command with conservative/idempotent Codex config writing against the installed `guild` binary, update the docs/ADR/working guidance to match, and keep proof flows/tests isolated from a real home directory before rerunning fmt/test/clippy plus the touched smoke coverage.
- 2026-03-19T21:18:36Z: The long workspace verification finished green while I was monitoring it: `cargo test --workspace` passed on the sane-local-defaults branch. A follow-on `cargo clippy --workspace --all-targets --all-features -- -D warnings` then surfaced only local doc-lint/style issues in the newly added Codex/path helpers, so I patched those comments plus one small `Option::or`/`map_or_else` cleanup before rerunning the final lint gate.
- 2026-03-19T22:42:11Z: Completed the sane-local-defaults/Codex-setup milestone. The `guild` CLI now resolves roots as `--registry-root` > `GUILD_REGISTRY_ROOT` > `~/.guild`, read-only commands use a non-initializing registry open path so they fail clearly without creating a fresh home root, `guild codex setup` prints or conservatively/idempotently writes Codex MCP config for the running `guild` binary, docs/ADR 0019/AGENTS all describe the new operator truth, and the updated tree passed `cargo fmt --all`, `cargo test --workspace`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, plus an isolated `HOME=$(mktemp -d) target/debug/guild codex setup --print` smoke.
- 2026-03-19T22:43:33Z: Follow-up pass after review feedback: add a first-class `guild init` workflow that can create the resolved `~/.guild` root and optionally fold in explicit Codex setup actions, and remove the unreleased `guild-codex`/`legacy` compatibility framing so docs and the CLI present one current supported path instead of two.
- 2026-03-19T23:14:47Z: Finished the follow-up pass. Added `guild init` as the explicit local bootstrap command with `--codex-print`, `--codex-global`, and `--codex-project` options; removed the extra unreleased `guild-codex` binary and simplified the Codex CLI surface to the single supported `guild codex ...` path; updated README/command-language/testing/ADR/AGENTS plus durable memory to present one current setup story; and revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli --test codex_workflow`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and an isolated `HOME=$(mktemp -d) target/debug/guild init --codex-global --codex-print` smoke.
- 2026-03-19T23:35:09Z: Starting the next CLI cleanup with product direction confirmed: fold the persistent Codex setup path fully into `guild init`, remove `guild codex setup`, and rewrite docs/help so `guild codex` is described as deterministic repo-local dogfood/testing rather than the normal operator setup path. This pass keeps scope narrow by not adding `guild test` yet.
- 2026-03-20T00:53:52Z: Finished the setup-surface cleanup. `guild init` is now the single current local setup workflow: it creates the selected Guild root, always prints the live Codex stdio wiring, and can write global or project Codex config with `--global` / `--project`. `guild codex setup` was removed, `guild codex` is now explicitly documented as deterministic repo-local dogfood/smoke coverage rather than operator setup, and the updated tree revalidated with `cargo fmt --all`, focused `guild-mcp` CLI/Codex tests, an isolated `HOME=$(mktemp -d) target/debug/guild init --global` smoke, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, and a fresh `cargo test --workspace`.
- 2026-03-20T00:56:39Z: Follow-up doc-alignment pass at user request: updated `SPECS.md`, `ARCHITECTURE.md`, `docs/testing.md`, ADR 0019, and AGENTS guidance so the contract-facing docs all describe the same current truth as the CLI and README: `guild` is the installed operator binary, root selection is `--registry-root` > `GUILD_REGISTRY_ROOT` > `~/.guild`, `guild init` is the only persistent local setup workflow, and `guild codex` remains the deterministic repo-local dogfood/smoke surface. No code-path changes were made in this pass.
- 2026-03-20T18:05:00Z: Started the draft-v1 M7 milestone implementation after a repo-grounded plan. Confirmed the bounded draft schema/control-plane work lives under `docs/schemas/draft-v1/`, the existing witness file is only a placeholder, M5 already has deterministic example-bounded observed-effect harnesses worth reusing, and the live Rust runner still does not persist a runtime-general exercised-authority event stream. Implementation scope is therefore the narrow honest path: draft-v1 witness generation and verification over bounded harness/fixture observations, explicit per-family coverage semantics, MAC-based protection reused from the draft token path, fixed claim verification without a DSL, updated checked examples, and docs that state the remaining runtime-general blocker plainly.
- 2026-03-20T19:20:27Z: Started the GitHub milestone pass. Confirmed the repo had epic issues for Patent Track M0-M10 but no actual GitHub milestone objects yet, so the update path is to create milestone objects, mirror the current repo-backed status in milestone descriptions, assign the matching epic issues, and remove the umbrella issue’s old “don’t use milestones” guidance so the GitHub tracking surface stops contradicting itself.
- 2026-03-20T19:27:38Z: Finished the GitHub milestone pass. Created 11 GitHub milestone objects for M0-M10 in `jkordish/Guild`, assigned each child epic issue (#2-#12) to its matching milestone, and updated umbrella issue #1 so milestone objects are now explicitly treated as roadmap summaries while the child epics remain the source of truth. Milestone descriptions were written conservatively against current repo-backed progress: M3/M4 as draft-v1 implemented, M5/M6/M7 as bounded draft-v1 implemented, M0/M2 as partial, and M1/M8/M9/M10 as not yet complete.
- 2026-03-20T19:31:20Z: Revalidated the current draft-v1 schema bundle before the requested commit/push step. `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py` passed on the working tree, including bundled admission, minimization, token, and M7 witness examples.
- 2026-03-20T20:52:00Z: Continued M8a runtime-alignment implementation after approval. Confirmed the validator already includes live-runtime alignment cases, the standard host Python lacks `referencing`, and the repo-local schema venv at `/tmp/guild-schema-venv` remains the right validation path. Next step is to run the full draft-v1 validator there, regenerate or patch any checked witness/examples that still encode the old `lossy`/`unmapped` semantics, then finish the documentation truth pass with exact supported-family and gap statements.
- 2026-03-20T21:24:00Z: Landed the core M8a alignment pass across the draft-v1 control-plane and live Rust observation seam. Added host-owned persistent `authority_observations` to `ExecutionRecord` plus live exercised/blocked recording for the active runtime families, introduced draft-v1 `runtime_alignment.py` with canonical live-family mapping (`exact` / `narrowing` / `partial` / `unsupported`) and coverage semantics, normalized live HTTP observations conservatively into the legacy `net.connect` alias while leaving unsupported live families as explicit unmapped observations, corrected the draft runtime examples and contract examples to the real `guild-skill-inspect-v1` world, bulk-regenerated the checked plan/proof/token/witness fixtures from current code, and revalidated the entire schema bundle with `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`. Remaining work in this pass is the docs truth update plus final Rust fmt/test verification on the touched crates.
- 2026-03-20T21:41:00Z: Finished the docs truth pass for M8a and reran the focused verification sweep. `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/compatibility_check.py`, `python3 -m py_compile ...`, and `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py` all passed. `cargo fmt --all` passed. `cargo test -p guild-registry --test local_registry` passed cleanly with the new `ExecutionRecord.authority_observations` field. The earlier focused Rust runner checks (`cargo test -p guild-runner --test http_requests --test inspect_slice`) had already passed after the runtime wiring landed; a later `cargo test -p guild-runner` sweep was still in the long Wasmtime-heavy compile/integration phase when I wrapped this pass, so the final report should call that out explicitly rather than pretending the full crate sweep finished.
- 2026-03-20T20:58:00Z: Finalized the M8a close-out pass requested after implementation. Updated GitHub tracking so umbrella issue #1 now marks M7 and M8a complete, issue #9 is closed as the bounded draft-v1 witness-layer epic, issue #10 is closed and retitled to `M8a Runtime Alignment and Canonical Effect Vocabulary`, and milestones 8 and 9 are renamed or closed to match. Cleared the last `clippy` complaint in `guild-runner` by extracting live HTTP parse/authorize helpers from the hostcall, then revalidated with `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo test -p guild-runner --test http_requests --test inspect_slice`, `/tmp/guild-schema-venv/bin/python docs/schemas/draft-v1/validate_examples.py`, `cargo test -p guild-registry --test local_registry`, `cargo fmt --all`, and `git diff --check`.
- 2026-03-20T21:05:00Z: Started a README language-tightening pass after the M8a landing and tracker cleanup. The goal is to make the top-level repo description read directly off the milestone vocabulary instead of mixing milestone-backed status with ad hoc roadmap phrasing. Planned scope is README-only: tighten the status and current-capability sections so readers can tell which parts are M3-M6 bounded draft-v1 work, which part is closed M7, which part is closed M8a, and which later milestones are still not started.
- 2026-03-20T20:54:34Z: Finished the README milestone-language pass. Tightened the top-level status wording, added an explicit `Milestone Status` section, and rewrote the “What Is Real Today” section so it separates the live runtime/transport slice from the bounded draft control-plane slice and names the current runtime-backed draft-v1 claim boundary directly. This was a docs-only follow-up and did not need a fresh test sweep before commit.
- 2026-03-20T20:56:28Z: Tightened the README opening language again so the repo identity and `Why Guild` section now follow the milestone program directly. The top-level description now frames Guild as the contracts-first runtime/control-plane layer for M3 through M8a, and `Why Guild` now explains the system in milestone terms: upper-bound admission, bounded minimization, invocation-bound delegation, durable witnesses, and live-runtime vocabulary alignment.
- 2026-03-20T20:58:28Z: Wrote a local `findings.md` summary covering the current repo state through M8a, as requested, and prepared the tracked README and working-memory edits for commit while intentionally keeping `findings.md` uncommitted.
- 2026-03-20T23:47:38Z: Started M8b direct canonical family support across the draft-v1 control plane. Repo truth confirmed from live Rust types and runner enforcement is narrower than the legacy draft alias vocabulary: `http-request` is already live but still alias-first in draft-v1, `read-resource`, `emit-evidence`, and `log-write` have canonical runtime observations plus enforceable host constraints but no direct draft-v1 effect shapes, and `invoke-skill` is live with alias-only scope semantics. Implementation will stay conservative and fail-closed: add direct canonical family authority specs keyed to the real Rust constraint fields, keep legacy aliases only as explicit compatibility/deprecation paths where exact or narrowing, thread the direct family language through admission/minimization/token/witness code without widening scope, publish a machine-readable per-family support matrix, and update examples/docs/tests so unsupported or partial negative-claim/minimization semantics remain explicit instead of implied away.
- 2026-03-21T00:16:10Z: M8b code wiring is in place across draft-v1 admission, token, runtime-alignment, and witness paths. Direct canonical `http-request`, `read-resource`, `invoke-skill`, `emit-evidence`, and `log-write` specs now exist in the shared schema bundle; live runtime observations map directly into those families instead of the old alias-first witness path; and the remaining validator drift is down to one stale checked minimization proof fixture whose deterministic IDs, TTL, and narrowed HTTP prefix no longer match the current normalized plan/request inputs. Next: refresh that checked proof, add the per-family support matrix, and update docs to state the exact remaining M5/M6/M7 blockers without overstating runtime generality.
- 2026-03-21T00:16:10Z: Finished the M8b draft-v1 control-plane pass. Shared schema defs now carry direct canonical authority selectors and scope shapes for `http-request`, `read-resource`, `invoke-skill`, `emit-evidence`, and `log-write`; M4 admission, M6 token issuance or verification, and M7 witness generation or verification now use those direct families end to end; legacy `net.connect` and `component.invoke` remain only as explicit deprecated narrowing aliases; checked examples now cover direct canonical live paths for all five active families plus alias rejection and fail-closed unmappable observations; and the docs now point at `docs/schemas/draft-v1/family_support_matrix.json` for the per-family or per-layer truth surface. Validation on the final tree: `python3 -m py_compile docs/schemas/draft-v1/{admission_core.py,token_core.py,runtime_alignment.py,witness_core.py,validate_examples.py,compatibility_check.py}`, `/tmp/guild-schema-venv-m8b/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv-m8b/bin/python docs/schemas/draft-v1/compatibility_check.py`, and `git diff --check` all pass. Remaining honest blockers after M8b: M5 live minimization is still unavailable for every canonical family, M6 remains a draft-local token layer rather than runtime-general enforcement, and M7 still has no fixed positive observed-fact claim vocabulary even though scope-only negative claims now work for the five active canonical families when coverage is complete.
- 2026-03-21T01:35:32Z: Started M8c live proof implementation. Reconfirmed the draft-v1 tree already carries the canonical five-family vocabulary from M8b while the Rust runner still has no live proof engine. Current conservative target is: `read-resource` gets bounded live proof through real Wasm executions over immutable Guild execution/object roots with explicit comparator normalization, `log-write` gets real live proof only if the runner can prove level-only shrinking honestly under the same real host path, and `http-request`, `invoke-skill`, and `emit-evidence` stay `not_proven` unless the runtime path and tests substantiate more. Planned wiring: add a runner-side live proof engine plus disposable proof scenario harness, then thread explicit live-vs-draft proof basis metadata through draft-v1 proof/token/witness validation and docs without weakening fail-closed behavior.
- 2026-03-21T20:33:50Z: Finalized the M8c `http-request` localhost `HEAD` explicit-port pass for commit and push. The runner now admits and classifies exactly one additional bounded live-proof-backed slice beyond the prior set: exact `localhost` + `HEAD` + explicit port with deterministic loopback-only resolution binding, exact observed path, no query/fragment, no redirects, one exercised request, normalized inspect-output comparison, and replay-fixture linkage. Draft-v1 examples, support matrix, validator checks, docs, and compatibility output were updated to match; `localhost` default-port `HEAD` and all broader hostname or HTTP shapes remain explicitly `not_proven`. Final verification before shipping: `cargo fmt --all`, focused and full `guild-runner` HTTP/live-proof tests, `cargo run -q -p guild-runner --example live_proof_scenarios -- http-request-localhost-head-bounded`, `/tmp/guild-schema-venv-m8c-head/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv-m8c-head/bin/python docs/schemas/draft-v1/compatibility_check.py`, and `git diff --check`.
- 2026-03-21T20:55:00Z: Added a local-only `findings.md` summary at user request. It captures the current repo truth after `bacbb0c`: what landed across M8a, M8b, and the partial M8c pass, the exact current live-proof envelope including the six proven `http-request` slices, the still-fail-closed gaps (`localhost` default-port forms, broader hostnames, query/fragment, redirects, multi-request, `https`, `invoke-skill`, `emit-evidence`), and the recommended next narrow technical work. Intentionally left uncommitted.
- 2026-03-21T21:20:00Z: Started the M8c invoke single-child feasibility implementation pass. Confirmed the honest supported slice has to be narrower than generic single-child invoke: one declared dependency alias resolved through the installed dependency snapshot to one exact child digest, fixed `guild-skill-inspect-v1`, deterministic child input, zero child-side authority use, zero nested child executions, and a child-aware comparator that loads the persisted child execution record rather than trusting parent traces alone.
- 2026-03-21T22:25:00Z: Finished the M8c invoke single-child feasibility pass. Landed one bounded live-proof-backed `invoke-skill` slice only for `invoke-parent-single-child -> child -> invoke-child-zero`: exactly one exercised declared alias, exact child digest from the installed dependency snapshot, fixed `guild-skill-inspect-v1`, deterministic child input, child-aware normalized comparator over the parent plus the persisted child execution record, zero child-side authority use, and zero nested child executions. Added stable invoke fail-closed reason codes, new zero-authority invoke fixtures, bounded plan -> proof -> token -> witness linkage for the supported slice, explicit upper-bound fallback and unlinked witness behavior for the unsupported multi-child slice, updated the draft-v1 support matrix/docs/examples, regenerated `compatibility_matrix.md`, and revalidated with `cargo test -p guild-runner --test live_proofs -- --nocapture`, `cargo test -p guild-runner --test composition -- --nocapture`, `cargo test -p guild-runner --test inspect_slice -- --nocapture`, `cargo fmt --all`, `python3 -m py_compile docs/schemas/draft-v1/*.py`, `/tmp/guild-schema-venv-invoke/bin/python docs/schemas/draft-v1/validate_examples.py`, `/tmp/guild-schema-venv-invoke/bin/python docs/schemas/draft-v1/compatibility_check.py`, and `git diff --check`.
- 2026-03-22T02:51:23Z: Started M8d rust-native truth-tooling implementation. Added a new workspace `xtask` plus internal crate `crates/guild-draft-truth`, landed Rust-native support-matrix generation/checking and compatibility-matrix generation/checking, added schema loading and validation helpers for the checked draft-v1 bundle, and wired a first Rust-native benchmark module that owns the slice and wall inventory, validates the checked benchmark/report artifacts, and regenerates them from Rust by calling the real `guild-runner` live-proof scenarios for the live path while keeping draft-layer token/witness behavior fail-closed and explicit. Regenerated `docs/schemas/draft-v1/family_support_matrix.json` and `docs/schemas/draft-v1/compatibility_matrix.md` from Rust. Remaining work in this pass is to finish benchmark parity, switch docs and workflow references to the Rust-native commands, and remove or deprecate the three Python truth entrypoints once the Rust path is the standard checked path.
- 2026-03-22T03:26:25Z: Ran a docs-truth pass after M8d. Re-read `README.md`, `docs/testing.md`, `docs/schemas/draft-v1/README.md`, `ARCHITECTURE.md`, and `docs/benchmarking/m8-real-path-benchmark.md`; aligned them with the actual `xtask` command surface (`truth`, `support-matrix`, `compatibility`, `benchmark`, each with `check|write` as shipped), removed stale Python-venv truth-pipeline wording from the main docs, and kept the old Python entrypoints documented only as deprecation wrappers or legacy manual harnesses rather than the standard repo truth path.
- 2026-03-22T04:42:00Z: Implemented `M8e-cli-ux-polish` on the real `guild` CLI without widening runtime semantics. Added primary `show` / `run` / `ls` / `get` / `why` / `verify` verbs with legacy `inspect` / `list` / `read` aliases preserved, introduced a shared CLI presenter for compact human output plus `--json` / `--porcelain` / verbosity / semantic color handling, added short-ref resolution for unambiguous skill refs plus `exec:` / `evidence:` / `obj:` prefixes, extended local registry reads with evidence/object listing helpers, updated canonical docs and example READMEs to teach the new happy path first, and added focused CLI regressions for human output, stdout/stderr split, machine flags, and short refs. Validation passed with `cargo fmt --all` and `cargo test -p guild-mcp --test guild_cli --test codex_workflow --test mcp_server_stdio`.
- 2026-03-22T05:22:24Z: Ran a CLI-doc accuracy pass after the M8e implementation. Re-checked `target/debug/guild --help` plus subcommand help for `show`, `run`, `ls`, `get`, `why`, and `verify`; corrected README wording to treat `run` rather than legacy `inspect` as the write-path root initializer and to call out `get` as the raw resource-read path; tightened `docs/command-language.md` so human verbosity/color controls are documented only for the styled summary commands while `get` is documented with `--json` / `--porcelain` / `--output`; and fixed the `examples/skills/explain-execution*` READMEs so their canonical proof flows now actually include installing and running the explain skills they describe instead of only the helper proofs.
- 2026-03-22T13:48:38Z: Started a direct dependency upgrade pass after dry-running `cargo upgrade -n --incompatible`. Applied the compatible workspace bumps (`wasmtime`, `wasmtime-wasi`, and `wasmtime-wasi-http` to `43.0.0`, `toml_edit` to `0.25`), confirmed that forcing `rand_core` to `0.10` breaks the current `ed25519-dalek`-backed key-generation path because `OsRng` and the `getrandom` feature are no longer exposed there, and reverted `rand_core` to the existing `0.6` line rather than widening the migration scope without a justified runtime or signing change.
- 2026-03-22T13:55:06Z: Patched the runner for the Wasmtime 43 WASI HTTP surface move by switching the host HTTP imports from the old crate root to `wasmtime_wasi_http::p2::*`. `cargo check --workspace` now passes again on the upgraded dependency graph. Next step is a fuller regression sweep to confirm the upgrade is safe beyond compilation.
- 2026-03-22T14:40:32Z: Finished the regression sweep for the direct dependency upgrade pass. `cargo test --workspace`, `cargo run -q -p xtask -- draft-v1 truth check`, and `git diff --check` all pass on the upgraded graph. Final direct dependency delta kept for this pass is `wasmtime`, `wasmtime-wasi`, and `wasmtime-wasi-http` at `43.0.0`, `toml_edit` at `0.25`, plus the runner import adjustment required by the Wasmtime 43 `p2` module move; `rand_core` stays on `0.6` because the current `ed25519-dalek` integration still depends on that line for `OsRng`-backed signing-key generation.
- 2026-03-23T05:21:26Z: Continued epic `#17` with task `#30` on `feature/ux-23-identity-layers`. Locked the user-facing output contract so README, `docs/command-language.md`, and shipped `guild` help all say the same thing: default human output is for reading, not parsing; `--json` and `--porcelain` are the stable machine surfaces; and low-noise human hints like `Next: ...` stay human-only. Added focused `guild_cli` regressions proving the machine modes for `install`, `show`, `run`, `why`, and `verify` do not leak `Next:` hints or verbose human-only resolution text, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T05:26:06Z: Continued epic `#17` with task `#31` on `feature/ux-23-identity-layers`. Chose `guild doctor` as the first read-only diagnostic command direction and documented that contract in README, `docs/command-language.md`, and shipped `guild help doctor` output without pretending the command already exists. Locked the scope to real Guild state only: selected root resolution, read-only access to installed and persisted state, local trust-store status, and Guild-specific setup/runtime checks grounded in real reads; locked the non-goals to no mutation, no bootstrap/repair side effects, and no generic machine-inspector or remote-registry behavior. Added focused `guild_cli` coverage for the new help topic and top-level help pointer, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T05:38:10Z: Continued epic `#17` with task `#32` on `feature/ux-23-identity-layers`. Reorganized the main user-facing docs and example READMEs around practical journeys rather than repo subsystems: README and `docs/command-language.md` now carry an explicit journey map; `examples/README.md` is grouped by install/run, explain, trust/transport, and debugging journeys; and the core example READMEs for `hello-inspect`, `explain-execution`, and `guild-ops-starter` now teach the primary CLI (`show`, `run`, `why`, `get`, `verify`) first while pushing cargo-based proof helpers into clearly secondary sections. Added a focused `guild_cli` regression that asserts those journey sections stay in the user-facing docs, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T05:38:10Z: Continued epic `#17` with task `#33` on `feature/ux-23-identity-layers`. Chose the first risky-flow preflight contract as `--preview` for `guild import bundle`, `guild import oci-layout`, and `guild pull`, then documented that direction in README, `docs/command-language.md`, and shipped CLI help via `guild help preview` plus the real Clap `--help` surfaces for import/pull/export/push. Locked the contract to real signed installed-state metadata, verification outcome, local trust posture, bundled closure scope, and would-import versus would-refuse reporting under the selected root; locked the non-goals to no root creation, no staging/install, no trust mutation, no fake detached summaries, and no first-slice preview contract for export or push. Added focused `guild_cli` coverage for the new help topic and transport help text, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T05:42:45Z: Final epic-17 conformance pass on `feature/ux-23-identity-layers`. Tightened the short daily-user explainer in `docs/how-guild-works.md` so it now also carries the final output-mode contract (`default` for reading, `--json` / `--porcelain` for machine reads) and points readers at the shipped future-facing help topics `guild help doctor` and `guild help preview`. Extended the existing user-facing-doc regression to lock that explainer alignment, reran `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`, and then moved to epic-tracker hygiene on GitHub so the issue graph reflects the real child-task spine and current branch-level completion.
- 2026-03-23T05:48:16Z: One more low-noise epic-17 refinement on `feature/ux-23-identity-layers`: extended human-only next-step hints on `guild show` where the follow-up is obvious. Skill summaries now point to `guild run <resolved-ref>` and `guild verify <resolved-ref>`, execution summaries point to `guild why exec:...`, and evidence summaries with a producing execution point to that same `guild why exec:...` path. JSON and porcelain output stay unchanged. Added focused `guild_cli` coverage for the new show-target hints, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T05:53:12Z: Closed the last obvious daily-command next-step gap inside epic `#17`: `guild verify` human output now points to `guild show -v <resolved-skill-ref>` so the trust check naturally leads into the identity/details view. Added explicit regression coverage for the new human hint and for `verify --json` staying machine-only, then reran `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`. The focused suite remains green at 41 passing tests on the current local branch state.
- 2026-03-23T05:58:01Z: Tightened the machine-surface side of the same epic-17 work after the new `guild show` hints landed. Extended the existing short-ref resource test so `show exec:... --json`, `show evidence:... --json`, `show obj:... --json`, and the matching porcelain paths are all explicitly asserted to stay free of human-only `Next:` hint text. Revalidated with `cargo test -p guild-mcp --test guild_cli` and `git diff --check`; the focused suite still passes cleanly with 41 tests.
- 2026-03-23T06:00:26Z: Finalized the human-output contract wording for epic `#17` so the short daily-user explainer and shipped `guild` help now explicitly match the behavior already present in the CLI: default human summaries are for reading, not parsing, and may include low-noise `Next:` hints when the follow-up is obvious. Updated `docs/how-guild-works.md`, the `show`/`run`/`why`/`verify` after-help text, and the focused `guild_cli` regressions together before rerunning `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T14:17:21Z: Addressed the remaining open PR review threads on `feature/ux-23-identity-layers`. Tightened `guild show -v` so it prints the full resolved digest instead of a short prefix, removed the unconditional `guild run` next-step hint from skill summaries, switched `guild why` follow-up hints to non-ambiguous full execution URIs, and corrected the shipped `run`/`import`/`pull` help text so it accurately describes the `--json` stream contract and clearly says planned `--preview` flags are not implemented yet. Updated the focused CLI regressions to lock those fixes, reran `cargo fmt --all` and `cargo test -p guild-mcp --test guild_cli` successfully, and still need to run `git diff --check`, push the branch, and resolve the GitHub threads.
- 2026-03-23T14:39:59Z: Addressed the next two open PR review threads on `feature/ux-23-identity-layers`. Human `Next:` hints now preserve the active non-default registry root by rendering `guild --registry-root <path> ...` in `show`, `run`, `why`, and `verify` follow-ups, so copy-paste guidance keeps pointing at the same local state the command just used. I also removed the install-time `Next: guild show -v ...` hint entirely because the current CLI does not yet expose a digest-pinned skill-ref grammar for `show`, making that version-only follow-up potentially ambiguous immediately after a same-version reinstall. Updated the focused `guild_cli` assertions accordingly, then reran `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check` successfully.
- 2026-03-23T17:15:00Z: Addressed the remaining PR review threads on `feature/ux-20-actionable-errors`. Root-qualified all registry-originated human follow-up hints at the command boundary so non-default `--registry-root` flows keep pointing at the same local state for `show`/`run`/`get`/`why`/`verify`/transport/trust commands, expanded the MCP error classifier so `*-not-granted` runtime capability denials land in `authority denial`, and reclassified `bundle-format-unsupported` as `runtime/compatibility` with compatible-version guidance instead of a trust/tamper hint. Added focused `guild_cli` coverage for root-qualified verify/get/install/import/pull follow-ups, a real runtime `http-request-path-not-granted` denial path, and unsupported bundle format imports, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T17:38:02Z: Addressed the next two PR review threads on `feature/ux-20-actionable-errors`. Missing-root read-only failures now run their recovery hint through the same root qualifier, so a missing non-default root points operators back at that exact root instead of silently steering them toward `~/.guild`. I also widened MCP authority-denial classification beyond suffix-only matching to cover grant-boundary wording like `read-resource-kind-denied`, `child-capability-mismatch`, and other “was/were not granted” or “exceeded the granted …” failures. Added focused `guild_cli` coverage for a missing custom-root `ls` failure and a real composite `child-capability-mismatch` run, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T17:56:51Z: Addressed the next trust-path PR review threads on `feature/ux-20-actionable-errors`. Tightened the CLI boundary so `guild trust remove <publisher-id>` reports a local lookup-style miss with a `guild trust list` follow-up instead of surfacing as a trust-verification problem, and reclassified broken local trust-store records (`trusted-publisher-read/parse/key/tier-*`) as `root/setup` with explicit repair guidance rather than blaming signed artifacts or publishers. Added focused `guild_cli` coverage for missing `trust remove` entries and a malformed local trusted publisher record, then revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T21:19:39Z: Addressed the two remaining open PR review threads on `feature/issue-19-why-navigation`. Human `guild why` nearby child/evidence navigation now renders stable accepted refs (`exec:<full-id>` / `evidence:<full-id>`) instead of 12-character prefixes, and verbose authority read-resource observations now preserve exact `guild://.../metadata` URIs rather than inventing an unparseable pseudo-ref. Updated the focused presenter/unit and `guild_cli` regressions together before rerunning `cargo fmt --all`, `cargo test -p guild-mcp --lib cli_presenter::tests`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T22:59:38Z: Addressed the next two PR review threads on `feature/issue-19-why-navigation`. Failed `invoke-skill` authority observations now keep their child `exec:<full-id>` ref even when a result error code is also present, so `guild why -v` still points operators at the exact persisted child execution. I also taught `ExecutionRecord` deserialization to remember whether `authority_observations` was actually recorded, which lets human `guild why` output say `authority: not-recorded` for legacy persisted executions instead of falsely claiming `none`; current records still serialize with the same public JSON shape. Revalidated with `cargo fmt --all`, `cargo test -p guild-registry --test local_registry`, `cargo test -p guild-runner --lib --no-run`, `cargo test -p guild-mcp --lib cli_presenter::tests`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T23:17:20Z: Addressed the next open PR review thread on `feature/issue-19-why-navigation`. `guild why -v` authority output for exercised `http-request` observations now preserves the returned HTTP status even when the guest later surfaces a result-error code, so operators can still distinguish transport success from post-response validation failure in the human explain path. Added a focused presenter regression for the combined `status 200 / http-response-not-json` case and revalidated with `cargo fmt --all`, `cargo test -p guild-mcp --lib cli_presenter::tests`, `cargo test -p guild-mcp --test guild_cli`, and `git diff --check`.
- 2026-03-23T23:36:03Z: Addressed the next PR review thread on `feature/issue-19-why-navigation` by carrying the authority-observation provenance bit across JSON and resource surfaces. `ExecutionRecord` now serializes `authority_observations_recorded`, deserialization preserves explicit values while still inferring `false` for legacy records missing both the bit and the observation field, and the shared ops/report support helpers now render legacy support as `not-recorded` instead of silently treating it as a recorded empty list. Updated the public contract notes in `SPECS.md` and `ARCHITECTURE.md`, added type-level roundtrip tests plus a CLI `guild get ... --json` assertion for the new field, and revalidated with `cargo fmt --all`, `cargo test -p guild-types`, `cargo test -p guild-mcp --lib cli_presenter::tests`, `cargo test -p guild-mcp --test guild_cli`, `cargo test -p guild-mcp --test inspect_facade --no-run`, and `git diff --check`.

{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "InspectHttpJsonInput",
  "type": "object",
  "additionalProperties": false,
  "required": ["url"],
  "properties": {
    "url": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute HTTP or HTTPS URL to fetch."
    },
    "method": {
      "type": "string",
      "enum": ["get", "head"],
      "description": "Optional HTTP method. Defaults to get."
    },
    "timeout_ms": {
      "type": "integer",
      "minimum": 1,
      "description": "Optional request timeout override in milliseconds."
    },
    "json_pointers": {
      "type": "array",
      "description": "Optional JSON Pointer paths to extract from the response body.",
      "items": {
        "type": "string"
      }
    }
  }
}

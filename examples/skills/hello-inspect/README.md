# hello-inspect

This example is intentionally small. It demonstrates the shape of a skill package without pretending the runtime stack already exists.

Idea:

- category: `explain`
- mode: inspect only
- output: a tiny structured payload and a human summary
- capabilities: none beyond logging

The manifest in this directory is illustrative and should evolve alongside the manifest contract.
